66. Devops
上节课和⼤家介绍了Gitlab CI 结合 Kubernetes 进⾏ CI/CD 的完整过程。这节课结合前⾯所学的知识
点给⼤家介绍⼀个完整的示例：使⽤ Jenkins + Gitlab + Harbor + Helm + Kubernetes 来实现⼀个完整
的 CI/CD 流⽔线作业。
其实前⾯的课程中我们就已经学习了 Jenkins Pipeline 与 Kubernetes 的完美结合，我们利⽤
Kubernetes 来动态运⾏ Jenkins 的 Slave 节点，可以和好的来解决传统的 Jenkins Slave 浪费⼤量资
源的缺点。之前的示例中我们是将项⽬放置在 Github 仓库上的，将 Docker 镜像推送到了 Docker
Hub，这节课我们来结合我们前⾯学习的知识点来综合运⽤下，使⽤ Jenkins、Gitlab、Harbor、
Helm、Kubernetes 来实现⼀个完整的持续集成和持续部署的流⽔线作业。
流程
下图是我们当前示例的流程图
ci/cd demo
Devops
574

1. 开发⼈员提交代码到 Gitlab 代码仓库
1. 通过 Gitlab 配置的 Jenkins Webhook 触发 Pipeline ⾃动构建
1. Jenkins 触发构建构建任务，根据 Pipeline 脚本定义分步骤构建
1. 先进⾏代码静态分析，单元测试
1. 然后进⾏ Maven 构建（Java 项⽬）
1. 根据构建结果构建 Docker 镜像
1. 推送 Docker 镜像到 Harbor 仓库
1. 触发更新服务阶段，使⽤ Helm 安装/更新 Release
1. 查看服务是否更新成功。
项⽬
本次示例项⽬是⼀个完整的基于 Spring Boot、Spring Security、JWT、React 和 Ant Design 构建的⼀
个开源的投票应⽤，项⽬地址：https://github.com/callicoder/spring-security-react-ant-design-polls-
app。
Devops
575

polling app1
我们将会在该项⽬的基础上添加部分代码，并实践 CI/CD 流程。
服务端
⾸先需要更改的是服务端配置，我们需要将数据库链接的配置更改成环境变量的形式，写死了的话就
没办法进⾏定制了，修改服务端⽂件 src/main/resources/application.properties ，将下⾯的数据库
配置部分修改成如下形式：
spring.datasource.url= jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:polling
_app}?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username= ${DB_USER:root}
spring.datasource.password= ${DB_PASSWORD:root}
Devops
576

当环境变量中有上⾯的数据配置的时候，就会优先使⽤环境变量中的值，没有的时候就会⽤默认的值
进⾏数据库配置。
由于我们要将项⽬部署到 Kubernetes 集群中去，所以我们需要将服务端进⾏容器化，所以我们在项⽬
根⽬录下⾯添加⼀个 Dockerfile ⽂件进⾏镜像构建：
FROM openjdk:8-jdk-alpine
MAINTAINER cnych <icnych@gmail.com>
ENV LANG en_US.UTF-8
ENV LANGUAGE en_US:en
ENV LC_ALL en_US.UTF-8
ENV TZ=Asia/Shanghai
RUN mkdir /app
WORKDIR /app
COPY target/polls-0.0.1-SNAPSHOT.jar /app/polls.jar
EXPOSE 8080
ENTRYPOINT ["java", "-Djava.security.egd=file:/dev/./urandom", "-jar","/app/polls.jar"]
由于服务端代码是基于 Spring Boot 构建的，所以我们这⾥使⽤⼀个 openjdk 的基础镜像，将打包过
后的 jar 包放⼊镜像之中，然后⽤过 java -jar 命令直接启动即可，这⾥就会存在⼀个问题了，我们
是在 Jenkins 的 Pipeline 中去进⾏镜像构建的，这个时候项⽬中并没有打包好的 jar 包⽂件，那么我
们应该如何获取打包好的 jar 包⽂件呢？这⾥我们可以使⽤两种⽅法：
第⼀种就是如果你⽤于镜像打包的 Docker 版本⼤于 17.06 版本的话，那么我墙裂推荐你使⽤ Docker
的多阶段构建功能来完成镜像的打包过程，我们只需要将上⾯的 Dockerfile ⽂件稍微更改下即可，将
使⽤ maven 进⾏构建的⼯作放到同⼀个⽂件中：
FROM maven:3.6-alpine as BUILD
COPY src /usr/app/src
COPY pom.xml /usr/app
RUN mvn -f /usr/app/pom.xml clean package -Dmaven.test.skip=true
FROM openjdk:8-jdk-alpine
MAINTAINER cnych <icnych@gmail.com>
ENV LANG en_US.UTF-8
ENV LANGUAGE en_US:en
ENV LC_ALL en_US.UTF-8
ENV TZ=Asia/Shanghai
RUN mkdir /app
WORKDIR /app
COPY --from=BUILD /usr/app/target/polls-0.0.1-SNAPSHOT.jar /app/polls.jar
Devops
577

EXPOSE 8080
ENTRYPOINT ["java", "-Djava.security.egd=file:/dev/./urandom", "-jar","/app/polls.jar"]
前⾯课程中我们就讲解过 Docker 的多阶段构建，这⾥我们定义了两个阶段，第⼀个阶段利
⽤ maven:3.6-alpine 这个基础镜像将我们的项⽬进⾏打包，然后将该阶段打包⽣成的 jar 包⽂件复
制到第⼆阶段进⾏最后的镜像打包，这样就可以很好的完成我们的 Docker 镜像的构建⼯作。
第⼆种⽅式就是我们传统的⽅式，在 Jenkins Pipeline 中添加⼀个 maven 构建的阶段，然后在第⼆个
Docker 构建的阶段就可以直接获取到前⾯的 jar 包了，也可以很⽅便的完成镜像的构建⼯作，为了
更加清楚的说明 Jenkins Pipeline 的⽤法，我们这⾥采⽤这种⽅式，所以 Dockerfile ⽂件还是使⽤第⼀
个就⾏。
现在我们可以将服务端的代码推送到 Gitlab 上去，我们这⾥的仓库地址
为：http://git.qikqiak.com/course/polling-app-server.git
注意，这⾥我们只推送的服务端代码。
客户端
客户端我们需要修改 API 的链接地址，修改⽂件 src/constants/index.js 中 API_BASE_URL 的地址，
我们同样通过环境变量来进⾏区分，如果有环境变量 APISERVER_URL ，则优先使⽤这个环境变量来作
为 API 请求的地址：
let API_URL = 'http://localhost:8080/api';
if (process.env.APISERVER_URL) {
    API_URL = `${process.env.APISERVER_URL}/api`;
}
export const API_BASE_URL = API_URL;
因为我们这⾥的项⽬使⽤的就是前后端分离的架构，所以我们同样需要将前端代码进⾏单独的部署，
同样我们要将项⽬部署到 Kubernetes 环境中，所以也需要做容器化，同样在项⽬根⽬录下⾯添加⼀
个 Dockerfile ⽂件：
FROM nginx:1.15.10-alpine
ADD build /usr/share/nginx/html
ADD nginx.conf
/etc/nginx/conf.d/default.conf
由于前端⻚⾯是单纯的静态⻚⾯，所以⼀般我们使⽤⼀个 nginx 镜像来运⾏，所以我们提供⼀
个 nginx.conf 配置⽂件：
server {
    gzip on;
    listen       80;
    server_name  localhost;
Devops
578

    root   /usr/share/nginx/html;
    location / {
        try_files $uri /index.html;
        expires 1h;
    }
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
这⾥我们可以看到我们需要将前⾯⻚⾯打包到⼀个 build ⽬录，然后将改⽬录添加到 nginx 镜像中
的 /usr/share/nginx/html ⽬录，这样当 nginx 镜像启动的时候就是直接使⽤的改⽂件夹下⾯的⽂件。
所以现在我们需要获取打包后的⽬录 build ，同样的，和上⾯服务端项⽬⼀样，我们可以使⽤两种⽅
式来完成这个⼯作。
第⼀种⽅式⾃然是推荐的 Docker 的多阶段构建，我们在⼀个 node 镜像的环境中就可以打包我们的前
端项⽬了，所以我们可以更改下 Dockerfile ⽂件，先进⾏ node 打包，然后再进⾏ nginx 启动：
FROM node:alpine as BUILD
WORKDIR /usr/src/app
RUN mkdir -p /usr/src/app
ADD . /usr/src/app
RUN npm install && \
    npm run build
FROM nginx:1.15.10-alpine
MAINTAINER cnych <icnych@gmail.com>
COPY --from=BUILD /usr/src/app/build /usr/share/nginx/html
ADD nginx.conf
/etc/nginx/conf.d/default.conf
第⼆种⽅式和上⾯⼀样在 Jenkins Pipeline 中添加⼀个打包构建的阶段即可，我们这⾥采⽤这种⽅式，
所以 Dockerfile ⽂件还是使⽤第⼀个就⾏。
现在我们可以将客户端的代码推送到 Gitlab 上去，我们这⾥的仓库地址
为：http://git.qikqiak.com/course/polling-app-client.git
Jenkins
现在项⽬准备好了，接下来我们可以开始 Jenkins 的配置，还记得前⾯在 Pipeline 结合 Kubernetes 的
课程中我们使⽤了⼀个 kubernetes 的 Jenkins 插件，但是之前使⽤的⽅式有⼀些不妥的地⽅，我们
Jenkins Pipeline 构建任务绑定到了⼀个固定的 Slave Pod 上⾯，这样就需要我们的 Slave Pod 中必须
Devops
579

包含⼀系列构建所需要的依赖，⽐如 docker、maven、node、java 等等，这样就难免需要我们⾃⼰定
义⼀个很庞⼤的 Slave 镜像，我们直接直接在 Pipeline 中去⾃定义 Slave Pod 中所需要⽤到的容器模
板，这样我们需要什么镜像只需要在 Slave Pod Template 中声明即可，完全不需要去定义⼀个庞⼤的
Slave 镜像了。
⾸先去掉 Jenkins 中 kubernetes 插件中的 Pod Template 的定义，Jenkins -> 系统管理 -> 系统设置 ->
云 -> Kubernetes区域，删除下⽅的 Kubernetes Pod Template  -> 保存。
jenkins kubernetes plugin
然后新建⼀个名为 polling-app-server 类型为 流⽔线(Pipeline) 的任务：
Devops
580

new pipeline task
然后在这⾥需要勾选 触发远程构建 的触发器，其中令牌我们可以随便写⼀个字符串，然后记住下⾯的
URL，将 JENKINS_URL 替换成 Jenkins 的地址,我们这⾥的地址就
是： http://jenkins.qikqiak.com/job/polling-app-server/build?token=server321 
trigger
Devops
581

然后在下⾯的 流⽔线 区域我们可以选择 Pipeline script 然后在下⾯测试流⽔线脚本，我们这⾥选
择 Pipeline script from SCM ，意思就是从代码仓库中通过 Jenkinsfile ⽂件获取 Pipeline
script 脚本定义，然后选择 SCM 来源为 Git ，在出现的列表中配置上仓库地
址 http://git.qikqiak.com/course/polling-app-server.git ，由于我们是在⼀个 Slave Pod 中去进⾏
构建，所以如果使⽤ SSH 的⽅式去访问 Gitlab 代码仓库的话就需要频繁的去更新 SSH-KEY，所以我
们这⾥采⽤直接使⽤⽤户名和密码的形式来⽅式：
pipeline scm
在 Credentials 区域点击 添加 按钮添加我们访问 Gitlab 的⽤户名和密码：
Devops
582

gitlab auth
然后需要我们配置⽤于构建的分⽀，如果所有的分⽀我们都想要进⾏构建的话，只需要将 Branch
Specifier 区域留空即可，⼀般情况下不同的环境对应的分⽀才需要构建，⽐如 master、develop、
test 等，平时开发的 feature 或者 bugfix 的分⽀没必要频繁构建，我们这⾥就只配置 master 和
develop 两个分⽀⽤户构建：
gitlab branch config
Devops
583

然后前往 Gitlab 中配置项⽬ polling-app-server  Webhook，settings -> Integrations，填写上⾯得到
的 trigger 地址：
webhook
保存后，可以直接点击 Test  ->  Push Event 测试是否可以正常访问 Webhook 地址，这⾥需要注意的
是我们需要配置下 Jenkins 的安全配置，否则这⾥的触发器没权限访问 Jenkins，系统管理 -> 全局安
全配置：取消 防⽌跨站点请求伪造 ，勾选上 匿名⽤户具有可读权限 ：
security config
如果测试出现了 Hook executed successfully: HTTP 201 则证明 Webhook 配置成功了，否则就需要检
查下 Jenkins 的安全配置是否正确了。
Devops
584

配置成功后我们只需要往 Gitlab 仓库推送代码就会触发 Pipeline 构建了。接下来我们直接在服务端代
码仓库根⽬录下⾯添加 Jenkinsfile ⽂件，⽤于描述流⽔线构建流程。
⾸先定义最简单的流程，要注意这⾥和前⾯课程的不同之处，这⾥我们使⽤ podTemplate 来定义不同
阶段使⽤的的容器，有哪些阶段呢？
Clone 代码 -> 代码静态分析 -> 单元测试 -> Maven 打包 -> Docker 镜像构建/推送 -> Helm 更新服
务。
Clone 代码在默认的 Slave 容器中即可；静态分析和单元测试我们这⾥直接忽略，有需要这个阶段的
同学⾃⼰添加上即可；Maven 打包肯定就需要 Maven 的容器了；Docker 镜像构建/推送是不是就需要
Docker 环境了呀；最后的 Helm 更新服务是不是就需要⼀个有 Helm 的容器环境了，所以我们这⾥就
可以很简单的定义 podTemplate 了，如下定义：(添加⼀个 kubectl ⼯具⽤于测试)
def label = "slave-${UUID.randomUUID().toString()}"
podTemplate(label: label, containers: [
  containerTemplate(name: 'maven', image: 'maven:3.6-alpine', command: 'cat', ttyEnabled: 
true),
  containerTemplate(name: 'docker', image: 'docker', command: 'cat', ttyEnabled: true),
  containerTemplate(name: 'kubectl', image: 'cnych/kubectl', command: 'cat', ttyEnabled: t
rue),
  containerTemplate(name: 'helm', image: 'cnych/helm', command: 'cat', ttyEnabled: true)
], volumes: [
  hostPathVolume(mountPath: '/root/.m2', hostPath: '/var/run/m2'),
  hostPathVolume(mountPath: '/home/jenkins/.kube', hostPath: '/root/.kube'),
  hostPathVolume(mountPath: '/var/run/docker.sock', hostPath: '/var/run/docker.sock')
]) {
  node(label) {
    def myRepo = checkout scm
    def gitCommit = myRepo.GIT_COMMIT
    def gitBranch = myRepo.GIT_BRANCH
    stage('单元测试') {
      echo "测试阶段"
    }
    stage('代码编译打包') {
      container('maven') {
        echo "代码编译打包阶段"
      }
    }
    stage('构建 Docker 镜像') {
      container('docker') {
        echo "构建 Docker 镜像阶段"
      }
    }
    stage('运⾏ Kubectl') {
      container('kubectl') {
        echo "查看 K8S 集群 Pod 列表"
        sh "kubectl get pods"
      }
    }
    stage('运⾏ Helm') {
      container('helm') {
        echo "查看 Helm Release 列表"
        sh "helm list"
      }
Devops
585

    }
  }
}
上⾯这段 groovy 脚本⽐较简单，我们需要注意的是 volumes 区域的定义，将容器中的 /root/.m2 ⽬
录挂载到宿主机上是为了给 Maven 构建添加缓存的，不然每次构建的时候都需要去重新下载依赖，这
样就⾮常慢了；挂载 .kube ⽬录是为了能够让 kubectl 和 helm 两个⼯具可以读取到 Kubernetes 集
群的连接信息，不然我们是没办法访问到集群的；最后挂载 /var/run/docker.sock ⽂件是为了能够让
我们的 docker 这个容器获取到 Docker Daemon 的信息的，因为 docker 这个镜像⾥⾯只有客户端的⼆
进制⽂件，我们需要使⽤宿主机的 Docker Daemon 来构建镜像，当然我们也需要在运⾏ Slave Pod 的
节点上拥有访问集群的⽂件，然后在每个 Stage 阶段使⽤特定需要的容器来进⾏任务的描述即可，所
以这⼏个 volumes 都是⾮常重要的
volumes: [
  hostPathVolume(mountPath: '/root/.m2', hostPath: '/var/run/m2'),
  hostPathVolume(mountPath: '/home/jenkins/.kube', hostPath: '/root/.kube'),
  hostPathVolume(mountPath: '/var/run/docker.sock', hostPath: '/var/run/docker.sock')
]
另外⼀个值得注意的就是 label 标签的定义，我们这⾥使⽤ UUID ⽣成⼀个随机的字符串，这样可以
让 Slave Pod 每次的名称都不⼀样，⽽且这样就不会被固定在⼀个 Pod 上⾯了，以后有多个构建任务
的时候就不会存在等待的情况了，这和我们之前的课程中讲到的固定在⼀个 label 标签上有所不同。
然后我们将上⾯的 Jenkinsfile ⽂件提交到 Gitlab 代码仓库上：
$ git add Jenkinsfile
$ git commit -m "添加 Jenkinsfile ⽂件"
$ git push origin master
然后切换到 Jenkins ⻚⾯上，正常情况就可以看到我们的流⽔线任务 polling-app-server 已经被触发
构建了，然后回到我们的 Kubernetes 集群中可以看到多了⼀个 slave 开头的 Pod，⾥⾯有5个容器，
就是我们上⾯ podTemplate 中定义的4个容器，加上⼀个默认的 jenkins slave 容器，同样的，构建任
务完成后，这个 Pod 也会被⾃动销毁掉：
$ kubectl get pods -n kube-ops
NAME                                                      READY     STATUS    RESTARTS   A
GE
jenkins-7fbfcc5ddc-xsqmt                                  1/1       Running   0          1
d
slave-6e898009-62a2-4798-948f-9c80c3de419b-0jwml-6t6hb   5/5       Running   0          36
s
......
正常可以看到 Jenkins 中的任务构建成功了：
Devops
586

build successfully
接下来的⼯作就是来实现上⾯具体的 Pipeline 脚本了。
Pipeline
第⼀个阶段：单元测试，我们可以在这个阶段是运⾏⼀些单元测试或者静态代码分析的脚本，我们这
⾥直接忽略。
第⼆个阶段：代码编译打包，我们可以看到我们是在⼀个 maven 的容器中来执⾏的，所以我们只需要
在该容器中获取到代码，然后在代码⽬录下⾯执⾏ maven 打包命令即可，如下所示：
  stage('代码编译打包') {
    try {
      container('maven') {
        echo "2. 代码编译打包阶段"
        sh "mvn clean package -Dmaven.test.skip=true"
      }
    } catch (exc) {
      println "构建失败 - ${currentBuild.fullDisplayName}"
      throw(exc)
    }
  }
Devops
587

第三个阶段：构建 Docker 镜像，要构建 Docker 镜像，就需要提供镜像的名称和 tag，要推送到
Harbor 仓库，就需要提供登录的⽤户名和密码，所以我们这⾥使⽤到了 withCredentials ⽅法，在⾥
⾯可以提供⼀个 credentialsId 为 dockerhub 的认证信息，如下：
container('构建 Docker 镜像') {
  withCredentials([[$class: 'UsernamePasswordMultiBinding',
    credentialsId: 'dockerhub',
    usernameVariable: 'DOCKER_HUB_USER',
    passwordVariable: 'DOCKER_HUB_PASSWORD']]) {
      container('docker') {
        echo "3. 构建 Docker 镜像阶段"
        sh """
          docker login ${dockerRegistryUrl} -u ${DOCKER_HUB_USER} -p ${DOCKER_HUB_PASSWORD
}
          docker build -t ${image}:${imageTag} .
          docker push ${image}:${imageTag}
          """
      }
  }
}
其中 ${image} 和 ${imageTag} 我们可以在上⾯定义成全局变量：
def imageTag = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
def dockerRegistryUrl = "registry.qikqiak.com"
def imageEndpoint = "course/polling-app-server"
def image = "${dockerRegistryUrl}/${imageEndpoint}"
docker 的⽤户名和密码信息则需要通过 凭据 来进⾏添加，进⼊ jenkins ⾸⻚ -> 左侧菜单 凭据  ->  添
加凭据 ，选择⽤户名和密码类型的，其中 ID ⼀定要和上⾯的 credentialsId 的值保持⼀致：
add docker hub credential
第四个阶段：运⾏ kubectl ⼯具，其实在我们当前使⽤的流⽔线中是⽤不到 kubectl ⼯具的，那么为什
么我们这⾥要使⽤呢？这还不是因为我们暂时还没有去写应⽤的 Helm Chart 包吗？所以我们先去⽤原
始的 YAML ⽂件来编写应⽤部署的资源清单⽂件，这也是我们写出 Chart 包前提，因为只有知道了应
Devops
588

⽤如何部署才可能知道 Chart 包如何编写，所以我们先编写应⽤部署资源清单。
⾸先当然就是 Deployment 控制器了，如下所示：（k8s.yaml）
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: polling-server
  namespace: course
  labels:
    app: polling-server
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: polling-server
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: myreg
      containers:
      - image: <IMAGE>:<IMAGE_TAG>
        name: polling-server
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: DB_HOST
          value: mysql
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: polling_app
        - name: DB_USER
          value: polling
        - name: DB_PASSWORD
          value: polling321
---
kind: Service
apiVersion: v1
metadata:
  name: polling-server
  namespace: course
spec:
  selector:
    app: polling-server
  type:  ClusterIP
  ports:
  - name: api-port
    port: 8080
    targetPort:  api
Devops
589

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mysql
  namespace: course
spec:
  template:
    metadata:
      labels:
        app: mysql
    spec:
      restartPolicy: Always
      containers:
      - name: mysql
        image: mysql:5.7
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3306
          name: dbport
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: rootPassW0rd
        - name: MYSQL_DATABASE
          value: polling_app
        - name: MYSQL_USER
          value: polling
        - name: MYSQL_PASSWORD
          value: polling321
        volumeMounts:
        - name: db
          mountPath: /var/lib/mysql
      volumes:
      - name: db
        hostPath:
          path: /var/lib/mysql
---
kind: Service
apiVersion: v1
metadata:
  name: mysql
  namespace: course
spec:
  selector:
    app: mysql
  type:  ClusterIP
  ports:
  - name: dbport
    port: 3306
    targetPort: dbport
可以看到我们上⾯的 YAML ⽂件中添加使⽤的镜像是⽤标签代替的： <IMAGE>:<IMAGE_TAG> ，这是因
为我们的镜像地址是动态的，下依赖我们在上⼀个阶段打包出来的镜像地址的，所以我们这⾥⽤标签
代替，然后将标签替换成真正的值即可，另外为了保证应⽤的稳定性，我们还在应⽤中添加了健康检
查，所以需要在代码中添加⼀个健康检查的 Controller：
（src/main/java/com/example/polls/controller/StatusController.java）
Devops
590

package com.example.polls.controller;
import org.springframework.web.bind.annotation.*;
@RestController
@RequestMapping("/api/_status/healthz")
public class StatusController {
    @GetMapping
    public String healthCheck() {
        return "UP";
    }
}
最后就是环境变量了，还记得前⾯我们更改了资源⽂件中数据库的配置吗？
（src/main/resources/application.properties）因为要尽量通⽤，我们在部署应⽤的时候很有可能已经
有⼀个外部的数据库服务了，所以这个时候通过环境变量传⼊进来即可。另外由于我们这⾥使⽤的是
私有镜像仓库，所以需要在集群中提前创建⼀个对应的 Secret 对象：
$ kubectl create secret docker-registry myreg --docker-server=registry.qikqiak.com --docke
r-username=DOCKER_USER --docker-password=DOCKER_PASSWORD --docker-email=DOCKER_EMAIL --nam
espace course
在代码根⽬录下⾯创建⼀个 manifests 的⽬录，⽤来存放上⾯的资源清单⽂件，正常来说是不是我们
只需要在镜像构建成功后，将上⾯的 k8s.yaml ⽂件中的镜像标签替换掉就 OK，所以这⼀步的动作如
下：
stage('运⾏ Kubectl') {
  container('kubectl') {
    echo "查看 K8S 集群 Pod 列表"
    sh "kubectl get pods"
    sh """
      sed -i "s/<IMAGE>/${image}" manifests/k8s.yaml
      sed -i "s/<IMAGE_TAG>/${imageTag}" manifests/k8s.yaml
      kubectl apply -f k8s.yaml
    """
  }
}
第五阶段：运⾏ Helm ⼯具，就是直接使⽤ Helm 来部署应⽤了，现在有了上⾯的基本的资源对象了，
要创建 Chart 模板就相对容易了，Chart 模板仓库地址：https://github.com/cnych/polling-helm，我们
可以根据 values.yaml ⽂件来进⾏⾃定义安装，模板中我们定义了可以指定使⽤外部数据库服务或者
内部独⽴的数据库服务，具体的我们可以去看模板中的定义。⾸先我们可以先使⽤这个模板在集群中
来测试下。⾸先在集群中 Clone 上⾯的 Chart 模板：
$ git clone https://github.com/cnych/polling-helm.git
然后我们使⽤内部的数据库服务，新建⼀个 custom.yaml ⽂件来覆盖 values.yaml ⽂件中的值：
Devops
591

persistence:
  enabled: true
  persistentVolumeClaim:
    database:
      storageClass: "database"
database:
  type: internal
  internal:
    database: "polling"
    # 数据库⽤户
    username: "polling"
    # 数据库⽤户密码
    password: "polling321"
可以看到我们这⾥使⽤了⼀个名为 database 的 StorgeClass 对象，所以还得创建先创建这个资源对
象：
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: database
provisioner: fuseim.pri/ifs
然后我们就可以在 Chart 根⽬录下⾯安装应⽤，执⾏下⾯的命令：
$ helm upgrade --install polling -f custom.yaml . --namespace course
Release "polling" does not exist. Installing it now.
NAME:   polling
LAST DEPLOYED: Sat May  4 23:31:42 2019
NAMESPACE: course
STATUS: DEPLOYED
RESOURCES:
==> v1/Pod(related)
NAME                                  READY  STATUS             RESTARTS  AGE
polling-polling-api-6b699478d6-lqwhw  0/1    ContainerCreating  0         0s
polling-polling-ui-587bbfb7b5-xr2ff   0/1    ContainerCreating  0         0s
polling-polling-database-0            0/1    Pending            0         0s
==> v1/Secret
NAME                      TYPE    DATA  AGE
polling-polling-database  Opaque  1     0s
==> v1/Service
NAME                      TYPE       CLUSTER-IP     EXTERNAL-IP  PORT(S)   AGE
polling-polling-api       ClusterIP  10.109.19.220  <none>       8080/TCP  0s
polling-polling-database  ClusterIP  10.98.136.190  <none>       3306/TCP  0s
polling-polling-ui        ClusterIP  10.108.170.43  <none>       80/TCP    0s
==> v1beta2/Deployment
NAME                 DESIRED  CURRENT  UP-TO-DATE  AVAILABLE  AGE
polling-polling-api  1        1        1           0          0s
polling-polling-ui   1        1        1           0          0s
==> v1/StatefulSet
Devops
592

NAME                      DESIRED  CURRENT  AGE
polling-polling-database  1        1        0s
==> v1beta1/Ingress
NAME                     HOSTS              ADDRESS  PORTS  AGE
polling-polling-ingress  ui.polling.domain  80       0s
NOTES:
1. Get the application URL by running these commands:
  http://ui.polling.domain
You have new mail in /var/spool/mail/root
注意我们这⾥安装也是使⽤的 helm upgrade 命令，这样有助于安装和更新的时候命令统⼀。
安装完成后，查看下 Pod 的运⾏状态：
$ kubectl get pods -n course
NAME                                   READY     STATUS    RESTARTS   AGE
polling-polling-api-6b699478d6-lqwhw   1/1       Running   0          3m
polling-polling-database-0             1/1       Running   0          3m
polling-polling-ui-587bbfb7b5-xr2ff    1/1       Running   0          3m
然后我们可以在本地 /etc/hosts ⾥⾯加上 http://ui.polling.domain 的的映射，这样我们就可以通
过这个域名来访问我们安装的应⽤了，可以注册、登录、发表投票内容了：
Devops
593

polling app
这样我们就完成了使⽤ Helm Chart 安装应⽤的过程，但是现在我们使⽤的包还是直接使⽤的 git 仓库
中的，平常我们正常安装的时候都是使⽤的 Chart 仓库中的包，所以我们需要将该 Chart 包上传到⼀
个仓库中去，⽐较幸运的是我们的 Harbor 也是⽀持 Helm Chart 包的。我们可以选择⼿动通过 Harbor
的 Dashboard 将 Chart 包进⾏上传，也可以通过使⽤ Helm Push 插件：
$ helm plugin install https://github.com/chartmuseum/helm-push
Downloading and installing helm-push v0.7.1 ...
https://github.com/chartmuseum/helm-push/releases/download/v0.7.1/helm-push_0.7.1_linux_am
d64.tar.gz
Installed plugin: push
当然我们需要⾸先将 Harbor 提供的仓库添加到 helm repo 中，由于是私有仓库，所以在添加的时候我
们需要添加⽤户名和密码：
$ helm repo add course https://registry.qikqiak.com/chartrepo/course --username=<harbor⽤户名
> --password=<harbor密码>
"course" has been added to your repositories
Devops
594

这⾥的 repo 的地址是 <Harbor URL>/chartrepo/<Harbor中项⽬名称> ，Harbor 中每个项⽬是分开的
repo，如果不提供项⽬名称，则默认使⽤ library 这个项⽬。
需要注意的是如果你的 Harbor 是采⽤的⾃建的 https 证书，这⾥就需要提供 ca 证书和私钥⽂件
了，否则会出现证书校验失败的错误 x509: certificate signed by unknown authority 。我们这
⾥是通过 cert-manager 为 Harbor 提供的⼀个信任的 https 证书，所以没有指定 ca 证书相关的
参数。
然后我们将上⾯的 polling-helm 这个 Chart 包上传到 Harbor 仓库中去：
$ helm push polling-helm course
Pushing polling-0.1.0.tgz to course...
Done.
这个时候我们登录的 Harbor 仓库中去，查看 course 这个项⽬下⾯的 Helm Charts 就可以发现多了⼀
个 polling 的应⽤了：
helm chart
我们也可以在右下⻆看到有添加仓库和安装 Chart 的相关命令。
到这⾥ Helm 相关的⼯作就准备好了。那么我们如何在 Jenkins Pipeline 中去使⽤ Helm 呢？我们可以
回顾下，我们平时的⼀个 CI/CD 的流程：开发代码 -> 提交代码 -> 触发镜像构建 -> 修改镜像tag -> 推
送到镜像仓库中去 -> 然后更改 YAML ⽂件镜像版本 -> 使⽤ kubectl ⼯具更新应⽤。
Devops
595

现在我们是不是直接使⽤ Helm 了，就不需要去⼿动更改 YAML ⽂件了，也不需要使⽤ kubectl ⼯具
来更新应⽤了，⽽是只需要去覆盖下 helm 中的镜像版本，直接 upgrade 是不是就可以达到应⽤更新
的结果了。我们可以去查看下 chart 包的 values.yaml ⽂件中关于 api 服务的定义：
api:
  image:
    repository: cnych/polling-api
    tag: 0.0.7
    pullPolicy: IfNotPresent
我们是不是只需要将上⾯关于 api 服务使⽤的镜像⽤我们这⾥ Jenkins 构建后的替换掉就可以了，这
样我们更改上⾯的最后 运⾏ Helm 的阶段如下：
stage('运⾏ Helm') {
  container('helm') {
    echo "更新 polling 应⽤"
    sh """
      helm upgrade --install polling polling --set persistence.persistentVolumeClaim.datab
ase.storageClass=database --set database.type=internal --set database.internal.database=po
lling --set database.internal.username=polling --set database.internal.password=polling321
 --set api.image.repository=${image} --set api.image.tag=${imageTag} --set imagePullSecret
s[0].name=myreg --namespace course
    """
  }
}
当然我们可以将需要更改的值都放⼊⼀个 YAML 之中来进⾏修改，我们这⾥通过 --set 来覆盖对应的
值，这样整个 API 服务的完整 Jenkinsfile ⽂件如下所示：
def label = "slave-${UUID.randomUUID().toString()}"
def helmLint(String chartDir) {
    println "校验 chart 模板"
    sh "helm lint ${chartDir}"
}
def helmInit() {
  println "初始化 helm client"
  sh "helm init --client-only --stable-repo-url https://mirror.azure.cn/kubernetes/charts/"
}
def helmRepo(Map args) {
  println "添加 course repo"
  sh "helm repo add --username ${args.username} --password ${args.password} course https:/
/registry.qikqiak.com/chartrepo/course"
  println "更新 repo"
  sh "helm repo update"
  println "获取 Chart 包"
  sh """
    helm fetch course/polling
    tar -xzvf polling-0.1.0.tgz
    """
Devops
596

}
def helmDeploy(Map args) {
    helmInit()
    helmRepo(args)
    if (args.dry_run) {
        println "Debug 应⽤"
        sh "helm upgrade --dry-run --debug --install ${args.name} ${args.chartDir} --set p
ersistence.persistentVolumeClaim.database.storageClass=database --set database.type=intern
al --set database.internal.database=polling --set database.internal.username=polling --set
 database.internal.password=polling321 --set api.image.repository=${args.image} --set api.
image.tag=${args.tag} --set imagePullSecrets[0].name=myreg --namespace=${args.namespace}"
    } else {
        println "部署应⽤"
        sh "helm upgrade --install ${args.name} ${args.chartDir} --set persistence.persist
entVolumeClaim.database.storageClass=database --set database.type=internal --set database.
internal.database=polling --set database.internal.username=polling --set database.internal
.password=polling321 --set api.image.repository=${args.image} --set api.image.tag=${args.t
ag} --set imagePullSecrets[0].name=myreg --namespace=${args.namespace}"
        echo "应⽤ ${args.name} 部署成功. 可以使⽤ helm status ${args.name} 查看应⽤状态"
    }
}
podTemplate(label: label, containers: [
  containerTemplate(name: 'maven', image: 'maven:3.6-alpine', command: 'cat', ttyEnabled: 
true),
  containerTemplate(name: 'docker', image: 'docker', command: 'cat', ttyEnabled: true),
  containerTemplate(name: 'helm', image: 'cnych/helm', command: 'cat', ttyEnabled: true)
], volumes: [
  hostPathVolume(mountPath: '/root/.m2', hostPath: '/var/run/m2'),
  hostPathVolume(mountPath: '/home/jenkins/.kube', hostPath: '/root/.kube'),
  hostPathVolume(mountPath: '/var/run/docker.sock', hostPath: '/var/run/docker.sock')
]) {
  node(label) {
    def myRepo = checkout scm
    def gitCommit = myRepo.GIT_COMMIT
    def gitBranch = myRepo.GIT_BRANCH
    def imageTag = sh(script: "git rev-parse --short HEAD", returnStdout: true).trim()
    def dockerRegistryUrl = "registry.qikqiak.com"
    def imageEndpoint = "course/polling-api"
    def image = "${dockerRegistryUrl}/${imageEndpoint}"
    stage('单元测试') {
      echo "1.测试阶段"
    }
    stage('代码编译打包') {
      try {
        container('maven') {
          echo "2. 代码编译打包阶段"
          sh "mvn clean package -Dmaven.test.skip=true"
        }
      } catch (exc) {
        println "构建失败 - ${currentBuild.fullDisplayName}"
        throw(exc)
      }
    }
    container('构建 Docker 镜像') {
      withCredentials([[$class: 'UsernamePasswordMultiBinding',
Devops
597

        credentialsId: 'dockerhub',
        usernameVariable: 'DOCKER_HUB_USER',
        passwordVariable: 'DOCKER_HUB_PASSWORD']]) {
          container('docker') {
            echo "3. 构建 Docker 镜像阶段"
            sh """
              docker login ${dockerRegistryUrl} -u ${DOCKER_HUB_USER} -p ${DOCKER_HUB_PASS
WORD}
              docker build -t ${image}:${imageTag} .
              docker push ${image}:${imageTag}
              """
          }
      }
    }
    stage('运⾏ Helm') {
      withCredentials([[$class: 'UsernamePasswordMultiBinding',
        credentialsId: 'dockerhub',
        usernameVariable: 'DOCKER_HUB_USER',
        passwordVariable: 'DOCKER_HUB_PASSWORD']]) {
          container('helm') {
            // todo，可以做分⽀判断
            echo "4. [INFO] 开始 Helm 部署"
            helmDeploy(
                dry_run     : false,
                name        : "polling",
                chartDir    : "polling",
                namespace   : "course",
                tag         : "${imageTag}",
                image       : "${image}",
                username    : "${DOCKER_HUB_USER}",
                password    : "${DOCKER_HUB_PASSWORD}"
            )
            echo "[INFO] Helm 部署应⽤成功..."
          }
      }
    }
  }
}
由于我们没有将 chart 包放⼊到 API 服务的代码仓库中，这是因为我们这⾥使⽤的 chart 包涉及到两个
应⽤，⼀个 API 服务，⼀个是前端展示的服务，所以我们这⾥是通过脚本⾥⾯去主动获取到 chart 包
来进⾏安装的，如果 chart 包跟随代码仓库⼀起管理当然就要简单许多了。
现在我们去更新 Jenkinsfile ⽂件，然后提交到 gitlab 中，然后去观察下 Jenkins 中的构建是否成功，
我们重点观察下 Helm 阶段：
Devops
598

jenkins helm console
当然我们还可以去做⼀些必要的判断⼯作，⽐如根据分⽀判断是否需要⾃动部署等等，同样也可以切
换到 Blue Occean 界⾯查看构建结果。
Devops
599

jenkins blue occean
现在⼤家可以尝试去修改下代码，然后提交代码到 gitlab 上，观察下 Jenkins 是否能够⾃动帮我们完
成整个 CI/CD 的过程。
作业：现在还有⼀个前端展示的项⽬：http://git.qikqiak.com/course/polling-app-client.git，⼤家针对这
个项⽬使⽤上⾯的 gitlab + jenkins + harbor + helm 来完成⼀个 Jenkins Pipeline 流⽔线的编写，尝试
去修改下前端⻚⾯内容，看是否能够⽣效。
Copyright © qikqiak.com 2018 all right reserved，powered by GitbookUpdated: 2019-05-05 09:19:18
Devops
600
