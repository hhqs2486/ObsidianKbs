# Claude Code 源码学习指南 — 第09章：UI 层

> **核心文件**：`src/ink/` · `src/main.tsx` · `src/components/` · `src/screens/` · `src/hooks/`

---

## 一、UI 架构概览

Claude Code 使用 **Ink**（React for Terminal）构建 TUI（Terminal UI）。Ink 允许用 React 组件和 hooks 来渲染终端 UI，支持完整的 React 生命周期。

```
用户终端（Terminal）
     │
     │  Ink 渲染层
     ▼
main.tsx（REPL 根组件，4,683 行）
  ├── AppStateProvider          全局状态
  ├── REPL 主界面               对话流
  │   ├── MessageList           消息列表（虚拟滚动）
  │   │   ├── AssistantMessage  LLM 回复渲染
  │   │   ├── UserMessage       用户输入渲染
  │   │   └── Tool UI 组件      工具调用/结果渲染
  │   ├── InputBox              用户输入框
  │   └── StatusBar             底部状态栏
  ├── TasksPanel                后台任务面板
  └── TeammatesPanel            Teammate 视图
```

---

## 二、Ink — React for Terminal

### 2.1 Ink 基础

```typescript
// src/ink/ink.ts

import { render, type Instance } from 'ink'

let inkInstance: Instance | null = null

// 启动 TUI
export function startInk(component: React.ReactNode): void {
  inkInstance = render(component, {
    stdout: process.stdout,
    debug: isDebugMode(),
    exitOnCtrlC: false,  // 手动处理 Ctrl+C
  })
}

// 停止 TUI（恢复终端）
export function stopInk(): void {
  inkInstance?.unmount()
  inkInstance = null
}
```

### 2.2 Ink 的工作原理

```
React 组件树
     │
     │ Ink 将 React 组件树转换为终端字节序列
     ▼
ANSI 转义码序列 → 发送到 stdout
     │
     ▼
终端渲染（支持颜色、光标控制、清屏等）
```

---

## 三、main.tsx — REPL 根组件

`main.tsx` 是整个 REPL 的根组件，也是整个项目最大的单文件之一（4,683 行）。它集成了所有核心功能：

```typescript
// src/main.tsx（简化版）

export function REPL(props: REPLProps) {
  // ── 状态 ──────────────────────────────────────
  const appState = useAppState()
  const { messages, tasks, verbose } = appState

  // ── 核心 hooks（100+ hooks）────────────────────
  const { submit, cancel } = useQueueProcessor()  // 消息队列处理
  const textInput = useTextInput()                // 用户输入框
  const terminalSize = useTerminalSize()          // 终端尺寸
  const { height, scrollToBottom } = useVirtualScroll()  // 虚拟滚动

  // ── 功能 hooks ────────────────────────────────
  useGlobalKeybindings()     // 全局快捷键
  useIDEIntegration()        // IDE 集成（VSCode 等）
  useMcpClients()            // MCP 客户端管理
  useScheduledTasks()        // 定时任务
  useSwarmInitialization()   // 蜂群模式
  useFileHistorySnapshotInit()  // 文件历史初始化

  // ── 渲染 ──────────────────────────────────────
  return (
    <Box flexDirection="column" height={terminalSize.rows}>
      <Box flex={1} flexDirection="column">
        <VirtualScrollContainer height={height}>
          {messages.map(msg => (
            <MessageRenderer key={msg.uuid} message={msg} tools={tools} />
          ))}
        </VirtualScrollContainer>
      </Box>

      {expandedView === 'tasks' && <TasksPanel tasks={tasks} />}
      {expandedView === 'teammates' && <TeammatesPanel />}

      <InputBox
        {...textInput}
        onSubmit={submit}
        placeholder="How can I help you today?"
      />

      <StatusBar />
    </Box>
  )
}
```

---

## 四、Tool UI 渲染生命周期

每个工具有完整的 UI 渲染生命周期，通过 Tool 接口的 `render*()` 方法实现：

### 4.1 渲染时序

```
① renderToolUseMessage(input, options)
   → 工具调用刚开始时显示（参数可能还未完整）
   → input 是 Partial<>（因为参数还在流式传输）

② renderToolUseProgressMessage(progress, options)
   → 工具执行过程中的实时进度
   → progress 包含工具推送的进度数据

③ renderToolUseTag(input)
   → 可选：工具调用旁边的标签（如超时、模型名）

④ renderToolResultMessage(content, progress, options)
   → 工具完成后的结果展示

   或

④' renderToolUseRejectedMessage(input, options)
   → 用户拒绝了工具调用

   或

④'' renderToolUseErrorMessage(result, options)
   → 工具调用失败
```

### 4.2 BashTool UI 示例

```typescript
// src/tools/BashTool/UI.tsx

export function renderToolUseMessage(
  input: Partial<BashInput>,
  options: RenderOptions,
): React.ReactNode {
  return (
    <Box>
      <Text color="cyan">⬢ Bash</Text>
      <Text> </Text>
      {input.command ? (
        <Text>{truncate(input.command, 80)}</Text>
      ) : (
        <Text color="gray">...</Text>  // 命令还在流式传输
      )}
    </Box>
  )
}

export function renderToolUseProgressMessage(
  progress: BashProgress[],
  options: { verbose: boolean },
): React.ReactNode {
  const latest = progress[progress.length - 1]
  if (!latest) return null

  return (
    <Box flexDirection="column">
      {latest.output.slice(-20).map((line, i) => (
        <Text key={i} color="gray">{line}</Text>
      ))}
    </Box>
  )
}

export function renderToolResultMessage(
  content: BashResult,
  progress: BashProgress[],
  options: { verbose: boolean; style?: 'condensed' },
): React.ReactNode {
  if (options.style === 'condensed') {
    return <Text color="green">✓ {truncate(content.stdout, 60)}</Text>
  }

  return (
    <Box flexDirection="column">
      {content.stdout && <Text>{content.stdout}</Text>}
      {content.stderr && <Text color="red">{content.stderr}</Text>}
      <Text color="gray">Exit code: {content.exitCode}</Text>
    </Box>
  )
}
```

---

## 五、虚拟滚动

消息列表可能非常长，全部渲染会导致性能问题。虚拟滚动只渲染可见区域：

```typescript
// src/hooks/useVirtualScroll.ts

export function useVirtualScroll(options: {
  totalItems: number
  itemHeight: number    // 每条消息的估计高度
  containerHeight: number
}) {
  const [scrollOffset, setScrollOffset] = useState(0)

  const visibleRange = useMemo(() => {
    const startIndex = Math.floor(scrollOffset / options.itemHeight)
    const visibleCount = Math.ceil(options.containerHeight / options.itemHeight)
    return {
      start: Math.max(0, startIndex - BUFFER_SIZE),
      end: Math.min(options.totalItems, startIndex + visibleCount + BUFFER_SIZE),
    }
  }, [scrollOffset, options])

  return {
    visibleRange,
    scrollToBottom: () => setScrollOffset(
      options.totalItems * options.itemHeight - options.containerHeight
    ),
    onScroll: (delta: number) => setScrollOffset(prev =>
      Math.max(0, prev + delta)
    ),
  }
}
```

---

## 六、输入系统

### 6.1 useTextInput — 核心输入 hook

```typescript
// src/hooks/useTextInput.ts

export function useTextInput() {
  const [value, setValue] = useState('')
  const [cursorPosition, setCursorPosition] = useState(0)

  // 处理键盘输入
  useInput((input, key) => {
    if (key.return) {
      handleSubmit(value)
      setValue('')
    } else if (key.backspace) {
      setValue(prev => prev.slice(0, -1))
    } else {
      setValue(prev => prev + input)
    }
  })

  return { value, setValue, cursorPosition }
}
```

### 6.2 Vim 模式支持

```typescript
// src/hooks/useVimInput.ts
// 完整的 vim 模式（Normal/Insert/Visual）

type VimMode = 'normal' | 'insert' | 'visual'

export function useVimInput() {
  const [mode, setMode] = useState<VimMode>('normal')

  useInput((input, key) => {
    if (mode === 'normal') {
      switch (input) {
        case 'i': setMode('insert'); break
        case 'A': goToEndOfLine(); setMode('insert'); break
        case 'dd': deleteLine(); break
        case 'yy': yankLine(); break
        case 'p': paste(); break
        // ... 更多 vim 命令
      }
    } else if (mode === 'insert') {
      if (key.escape) setMode('normal')
      else handleNormalInput(input, key)
    }
  })
}
```

### 6.3 历史搜索

```typescript
// src/hooks/useHistorySearch.ts

export function useHistorySearch() {
  const [searchMode, setSearchMode] = useState(false)
  const [query, setQuery] = useState('')
  const history = useConversationHistory()

  // Ctrl+R 激活
  useInput((input, key) => {
    if (key.ctrl && input === 'r') {
      setSearchMode(true)
    }
  })

  const results = useMemo(
    () => history.filter(msg => msg.content.includes(query)),
    [history, query],
  )

  return { searchMode, query, setQuery, results }
}
```

### 6.4 粘贴处理

```typescript
// src/hooks/usePasteHandler.ts

export function usePasteHandler() {
  useInput((input, key) => {
    if (isPasteEvent(key)) {
      if (isImageData(input)) {
        // 图片粘贴：提取 base64 数据，创建图片附件
        handleImagePaste(input)
      } else {
        // 文本粘贴：正常处理
        handleTextPaste(input)
      }
    }
  })
}
```

---

## 七、Spinner 和加载状态

```typescript
// src/components/Spinner.tsx

export type SpinnerMode =
  | 'thinking'   // Claude 正在思考
  | 'loading'    // 工具执行中
  | 'streaming'  // 流式输出中

const SPINNER_FRAMES = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏']

export function Spinner({ mode }: { mode: SpinnerMode }) {
  const [frame, setFrame] = useState(0)

  useEffect(() => {
    const interval = setInterval(() => {
      setFrame(prev => (prev + 1) % SPINNER_FRAMES.length)
    }, 80)
    return () => clearInterval(interval)
  }, [])

  const color = mode === 'thinking' ? 'cyan' : mode === 'loading' ? 'yellow' : 'green'
  return <Text color={color}>{SPINNER_FRAMES[frame]}</Text>
}
```

---

## 八、Screens — 全屏视图

### 8.1 Tasks Panel

```typescript
// src/screens/TasksPanel.tsx

export function TasksPanel({ tasks }: { tasks: TaskState[] }) {
  const running = tasks.filter(t => t.status === 'running')
  const completed = tasks.filter(t => isTerminalTaskStatus(t.status))

  return (
    <Box flexDirection="column" padding={1}>
      <Text bold>Background Tasks ({running.length} running)</Text>

      {running.map(task => (
        <TaskRow key={task.id} task={task} />
      ))}

      {completed.length > 0 && (
        <>
          <Text color="gray">─── Completed ───</Text>
          {completed.map(task => (
            <TaskRow key={task.id} task={task} />
          ))}
        </>
      )}
    </Box>
  )
}
```

### 8.2 MessageSelector

```typescript
// src/components/MessageSelector.tsx
// Ctrl+R 类似的消息历史跳转

export function MessageSelector({ onSelect }: { onSelect: (idx: number) => void }) {
  const messages = useMessages()
  const [searchQuery, setSearchQuery] = useState('')

  const filtered = messages.filter(msg =>
    extractText(msg).toLowerCase().includes(searchQuery.toLowerCase())
  )

  return (
    <Box flexDirection="column">
      <TextInput value={searchQuery} onChange={setSearchQuery} placeholder="Search messages..." />
      <Box flexDirection="column">
        {filtered.map((msg, i) => (
          <MessagePreview
            key={msg.uuid}
            message={msg}
            onSelect={() => onSelect(i)}
          />
        ))}
      </Box>
    </Box>
  )
}
```

---

## 九、IDE 集成

```typescript
// src/hooks/useIDEIntegration.tsx
// 与 VSCode、Cursor、JetBrains 等 IDE 集成

export function useIDEIntegration() {
  // 监听来自 IDE 插件的消息（通过 MCP 或 WebSocket）
  useEffect(() => {
    const connection = connectToIDE()

    connection.on('selection', (selection) => {
      // IDE 中的代码选中 → 自动注入到对话上下文
      injectSelectionContext(selection)
    })

    connection.on('file_open', (file) => {
      // 当前打开的文件 → 作为上下文
      updateOpenFileContext(file)
    })

    return () => connection.disconnect()
  }, [])
}
```

---

## 十、outputStyles — 输出样式系统

```typescript
// src/outputStyles/

// 支持多种输出样式：
// - 'auto'：自动检测终端能力
// - 'full'：完整颜色和格式
// - 'minimal'：最小化格式（适合截图/日志）
// - 'json'：JSON 格式输出（SDK 模式）

export type OutputStyle = 'auto' | 'full' | 'minimal' | 'json'

export function getOutputStyle(): OutputStyle {
  if (isNonInteractiveSession()) return 'json'
  if (isColorDisabled()) return 'minimal'
  return 'full'
}
```

---

*上一章：[08-服务层](./08-服务层.md) | 下一章：[10-CLI 入口](./10-CLI入口.md)*
