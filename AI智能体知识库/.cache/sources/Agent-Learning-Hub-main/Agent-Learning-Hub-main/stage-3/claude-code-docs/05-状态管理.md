# Claude Code 源码学习指南 — 第05章：状态管理

> **核心文件**：`src/state/AppStateStore.ts` · `src/state/store.ts` · `src/state/AppState.tsx` · `src/state/selectors.ts`

---

## 一、状态管理架构概览

Claude Code 使用**自定义 Store + React Context** 的状态管理方案，类似于 Zustand 但针对 Ink（Terminal UI）场景定制：

```
                    AppStateStore
                         │
                    React Context
                    (AppStoreContext)
                         │
            ┌────────────┼────────────┐
            │            │            │
         UI 组件      工具调用      后台任务
    useSyncExternalStore  getAppState  setAppStateForTasks
```

**核心设计原则**：
1. **DeepImmutable**：所有状态不可变，防止意外突变
2. **函数式更新**：`setState(prev => next)` 模式，保证原子性
3. **单向数据流**：状态 → UI，UI 事件 → 更新状态

---

## 二、AppState 完整结构

```typescript
// src/state/AppStateStore.ts
export type AppState = DeepImmutable<{
  // ─── 基础配置 ────────────────────────────────────
  settings: SettingsJson              // 用户设置（~/.claude/settings.json）
  verbose: boolean                    // 详细模式
  isBriefOnly: boolean               // 只显示简报
  mainLoopModel: ModelSetting        // 主循环使用的模型
  mainLoopModelForSession: ModelSetting  // 会话内锁定的模型
  statusLineText: string | undefined // 状态栏文字

  // ─── 权限 ────────────────────────────────────────
  toolPermissionContext: ToolPermissionContext  // 当前权限上下文

  // ─── 对话历史 ────────────────────────────────────
  messages: Message[]                // 完整对话历史

  // ─── 异步任务 ────────────────────────────────────
  tasks: Map<string, TaskState>      // taskId → TaskState

  // ─── MCP 连接 ────────────────────────────────────
  mcpClients: MCPServerConnection[]  // 已连接的 MCP 服务器
  mcpResources: Record<string, ServerResource[]>

  // ─── 插件和 Agent ────────────────────────────────
  loadedPlugins: LoadedPlugin[]      // 已加载插件
  pluginErrors: PluginError[]        // 插件错误
  agentDefinitions: AgentDefinitionsResult  // 可用的 Agent 定义

  // ─── UI 状态 ─────────────────────────────────────
  expandedView: 'none' | 'tasks' | 'teammates'  // 展开的视图
  selectedIPAgentIndex: number       // 选中的 Teammate 索引
  footerItem: FooterItem | undefined // 底部显示的项目

  // ─── 高级特性 ────────────────────────────────────
  speculationState: SpeculationState // 预测执行状态
  fileHistoryState: FileHistoryState // 文件历史（撤销支持）
  attributionState: AttributionState // Git 提交归因
  todoList?: TodoList                // 当前 Todo 列表

  // ─── 会话 hooks ──────────────────────────────────
  sessionHooksState: SessionHooksState  // 会话级 hooks 状态
  denialTrackingState: DenialTrackingState  // 权限拒绝追踪

  // ─── 通知和权限队列 ──────────────────────────────
  notifications: Notification[]
  pendingPermissions: OrphanedPermission[]
  elicitationEvents: ElicitationRequestEvent[]

  // ─── 其他 UI 状态 ────────────────────────────────
  inProgressToolUseIDs: Set<string>  // 正在执行的工具调用 ID
  hasInterruptibleToolInProgress: boolean
  isLoading: boolean
  isFetchingResponse: boolean
}>
```

---

## 三、DeepImmutable — 类型级不可变

```typescript
// src/types/utils.ts
// 递归地将所有属性标记为 readonly
export type DeepImmutable<T> =
  T extends Map<infer K, infer V>
    ? ReadonlyMap<K, DeepImmutable<V>>
  : T extends Set<infer V>
    ? ReadonlySet<DeepImmutable<V>>
  : T extends object
    ? { readonly [P in keyof T]: DeepImmutable<T[P]> }
  : T
```

**为什么要 DeepImmutable**：

```typescript
// ❌ 这会被 TypeScript 拒绝
appState.verbose = true  // Error: Cannot assign to 'verbose' (readonly)
appState.messages.push(msg)  // Error: push does not exist on ReadonlyArray

// ✅ 必须通过 setState 更新
setAppState(prev => ({
  ...prev,
  verbose: true,
  messages: [...prev.messages, msg],  // 创建新数组
}))
```

---

## 四、Store 实现

```typescript
// src/state/store.ts
export type Store<T> = {
  getState(): T
  setState(f: (prev: T) => T): void
  subscribe(listener: () => void): () => void  // 返回取消订阅函数
}

export function createStore<T>(
  initialState: T,
  onChange?: (args: { newState: T; oldState: T }) => void,
): Store<T> {
  let state = initialState
  const listeners = new Set<() => void>()

  return {
    getState: () => state,

    setState: (f) => {
      const oldState = state
      const newState = f(state)
      if (newState === oldState) return  // 引用相等则跳过

      state = newState
      onChange?.({ newState, oldState })  // 触发变更回调
      listeners.forEach(l => l())         // 通知所有订阅者
    },

    subscribe: (listener) => {
      listeners.add(listener)
      return () => listeners.delete(listener)  // 返回取消订阅函数
    },
  }
}
```

---

## 五、AppStateProvider — React 集成

```typescript
// src/state/AppState.tsx

export function AppStateProvider({ children, initialState, onChangeAppState }) {
  // 禁止嵌套（保证单例）
  const hasAppStateContext = useContext(HasAppStateContext)
  if (hasAppStateContext) {
    throw new Error("AppStateProvider can not be nested within another AppStateProvider")
  }

  // 创建 Store（只创建一次）
  const [store] = useState(() =>
    createStore(initialState ?? getDefaultAppState(), onChangeAppState)
  )

  return (
    <HasAppStateContext.Provider value={true}>
      <AppStoreContext.Provider value={store}>
        <MailboxProvider>
          <VoiceProvider>
            {children}
          </VoiceProvider>
        </MailboxProvider>
      </AppStoreContext.Provider>
    </HasAppStateContext.Provider>
  )
}
```

---

## 六、读写状态的标准方式

### 6.1 在 React 组件中

```typescript
// 读取状态（响应式，状态变化自动重渲染）
import { useSyncExternalStore } from 'react'
import { useContext } from 'react'

function MyComponent() {
  const store = useContext(AppStoreContext)!
  const state = useSyncExternalStore(store.subscribe, store.getState)

  return <Text>{state.verbose ? 'Verbose' : 'Normal'}</Text>
}

// 使用 selectors 读取特定字段（避免不必要重渲染）
// src/state/selectors.ts
export const useVerbose = () => useSelector(state => state.verbose)
export const useTasks = () => useSelector(state => state.tasks)
```

### 6.2 在工具调用中（非 React）

```typescript
// ToolUseContext 提供直接访问
async function myToolCall(args, context: ToolUseContext) {
  // 读取
  const state = context.getAppState()
  const isVerbose = state.verbose

  // 更新（函数式，不可变）
  context.setAppState(prev => ({
    ...prev,
    statusLineText: 'Processing...',
  }))
}
```

### 6.3 子 Agent 中的状态更新

```typescript
// 子 Agent 的 setAppState 是 no-op（状态隔离）
// 但 setAppStateForTasks 可以更新 Task 状态（跨 Agent 共享）
async function subAgentWork(context: ToolUseContext) {
  // ❌ 这不会生效（no-op）
  context.setAppState(prev => ({ ...prev, verbose: true }))

  // ✅ 这会生效（更新 Task 状态，传递到根 Store）
  context.setAppStateForTasks?.(prev => ({
    ...prev,
    tasks: updateTask(prev.tasks, taskId, { status: 'completed' }),
  }))
}
```

---

## 七、SpeculationState — 预测执行

Speculation 是 Claude Code 最独特的性能优化之一：

```typescript
// src/state/AppStateStore.ts

export type SpeculationState =
  | { status: 'idle' }
  | {
      status: 'active'
      id: string                              // 预测 ID（用于匹配确认）
      abort: () => void                       // 取消预测的函数
      startTime: number                       // 预测开始时间
      messagesRef: { current: Message[] }     // 预测的消息（Mutable Ref 避免数组展开开销）
      writtenPathsRef: { current: Set<string> } // 预测写入的文件路径（Overlay）
      boundary: CompletionBoundary | null     // 预测完成边界
      suggestionLength: number                // 建议文本长度
      toolUseCount: number                    // 工具调用次数
      isPipelined: boolean                    // 是否是流水线预测
      contextRef: { current: REPLHookContext }  // Mutable Ref（避免闭包陈旧）
      pipelinedSuggestion?: {...} | null      // 流水线建议
    }

export const IDLE_SPECULATION_STATE: SpeculationState = { status: 'idle' }
```

**Speculation 工作原理**：

```
用户输入文字（还没按回车）
         │
         ▼
系统基于输入文字预测下一步操作
  └── 例如：预测用户会让 Claude 读取 src/foo.ts
         │
         ▼
提前执行预测的工具调用
  └── FileReadTool("src/foo.ts") → 结果写入 writtenPathsRef（Overlay）
         │
         ▼
用户按回车确认消息
  ├── 预测正确：直接应用 Overlay 结果（节省 LLM 调用时间）
  └── 预测错误：丢弃 Overlay，正常执行
```

---

## 八、CompletionBoundary — 完成边界

```typescript
// src/state/AppStateStore.ts

export type CompletionBoundary =
  | { type: 'complete'; completedAt: number; outputTokens: number }
  | { type: 'bash'; command: string; completedAt: number }
  | { type: 'edit'; toolName: string; filePath: string; completedAt: number }
  | { type: 'denied_tool'; toolName: string; detail: string; completedAt: number }
```

CompletionBoundary 记录上一个操作的"完成边界"，用于：
- Speculation 判断是否需要启动新的预测
- UI 显示上一操作的摘要信息
- 分析工具调用模式

---

## 九、FileHistoryState — 文件变更追踪

```typescript
// src/utils/fileHistory.ts

// 记录每次文件编辑的快照（支持撤销）
export type FileHistoryState = {
  snapshots: Map<string, FileSnapshot>  // filePath → 快照
  editCount: number
}

// 使用
export function fileHistoryEnabled(): boolean {
  return getFeatureValue_CACHED_MAY_BE_STALE('FILE_HISTORY')
}

export async function fileHistoryMakeSnapshot(
  filePath: string,
  context: ToolUseContext,
): Promise<void> {
  if (!fileHistoryEnabled()) return

  const content = await readFile(filePath)
  context.updateFileHistoryState(prev => ({
    ...prev,
    snapshots: new Map([...prev.snapshots, [filePath, { content, timestamp: Date.now() }]])
  }))
}
```

---

## 十、getDefaultAppState() — 初始状态

```typescript
// src/state/AppStateStore.ts

export function getDefaultAppState(): AppState {
  return {
    settings: getInitialSettings(),
    verbose: false,
    isBriefOnly: false,
    mainLoopModel: getDefaultModel(),
    mainLoopModelForSession: getDefaultModel(),
    statusLineText: undefined,
    toolPermissionContext: getEmptyToolPermissionContext(),
    messages: [],
    tasks: new Map(),
    mcpClients: [],
    mcpResources: {},
    loadedPlugins: [],
    pluginErrors: [],
    agentDefinitions: { builtin: [], custom: [] },
    expandedView: 'none',
    selectedIPAgentIndex: 0,
    footerItem: undefined,
    speculationState: IDLE_SPECULATION_STATE,
    fileHistoryState: { snapshots: new Map(), editCount: 0 },
    attributionState: createEmptyAttributionState(),
    todoList: undefined,
    sessionHooksState: {},
    denialTrackingState: { counts: new Map() },
    notifications: [],
    pendingPermissions: [],
    elicitationEvents: [],
    inProgressToolUseIDs: new Set(),
    hasInterruptibleToolInProgress: false,
    isLoading: false,
    isFetchingResponse: false,
  }
}
```

---

*上一章：[04-Task 系统](./04-Task系统.md) | 下一章：[06-权限系统](./06-权限系统.md)*
