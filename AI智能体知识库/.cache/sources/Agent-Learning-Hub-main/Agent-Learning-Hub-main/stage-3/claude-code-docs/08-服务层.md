# Claude Code 源码学习指南 — 第08章：服务层

> **核心文件**：`src/services/` — API、Compact、Analytics、Memory、Plugin、AgentSummary 等

---

## 一、服务层概览

`src/services/` 是支撑整个系统运行的基础设施层。服务通过 `ToolUseContext` 注入，对工具和 Agent 透明可用。

```
src/services/
├── AgentSummary/           # 子 Agent 完成摘要
├── MagicDocs/              # 文档生成服务
├── PromptSuggestion/       # 实时操作建议
├── SessionMemory/          # 会话内记忆提取
├── analytics/              # 埋点和 Feature Flags
├── api/                    # Claude API 调用层
├── autoDream/              # 自主任务服务（KAIROS）
├── awaySummary.ts          # 离开时摘要
├── claudeAiLimits.ts       # API 限额管理
├── compact/                # Context 压缩
├── diagnosticTracking.ts   # 诊断追踪
├── extractMemories/        # 记忆提取 AI
├── internalLogging.ts      # 内部日志
├── lsp/                    # Language Server Protocol
├── mcp/                    # MCP 协议实现
├── mcpServerApproval.tsx   # MCP 服务器审批 UI
├── notifier.ts             # OS 通知
├── oauth/                  # OAuth 认证
├── plugins/                # 插件加载
├── policyLimits/           # 策略限制
├── preventSleep.ts         # 防止系统休眠
├── rateLimitMessages.ts    # 限流消息
├── remoteManagedSettings/  # 远程管理设置
├── settingsSync/           # 设置同步
├── teamMemorySync/         # 团队记忆同步
├── tips/                   # 使用提示
├── tokenEstimation.ts      # Token 估算
├── toolUseSummary/         # 工具使用摘要
├── tools/                  # 工具编排层
│   ├── StreamingToolExecutor.ts  # 流式工具执行器
│   └── toolOrchestration.ts     # 工具并发编排
└── vcr.ts                  # 请求录制（测试）
```

---

## 二、API 层 — Claude API 调用

### 2.1 claude.ts — 核心 API 客户端

```typescript
// src/services/api/claude.ts

// token 用量累积（跨多次 API 调用汇总）
export function accumulateUsage(
  existing: NonNullableUsage,
  newUsage: Usage,
): NonNullableUsage {
  return {
    input_tokens: existing.input_tokens + (newUsage.input_tokens ?? 0),
    output_tokens: existing.output_tokens + (newUsage.output_tokens ?? 0),
    cache_read_input_tokens: existing.cache_read_input_tokens + (newUsage.cache_read_input_tokens ?? 0),
    cache_creation_input_tokens: existing.cache_creation_input_tokens + (newUsage.cache_creation_input_tokens ?? 0),
  }
}

// 更新 usage（用于 UI 显示）
export function updateUsage(
  messages: Message[],
  usage: Usage,
): Message[] {
  // 在最后一条 assistant 消息上记录 usage
  return messages.map((m, i) =>
    i === messages.length - 1 && m.type === 'assistant'
      ? { ...m, usage }
      : m
  )
}
```

### 2.2 errors.ts — 错误分类

```typescript
// src/services/api/errors.ts

export function categorizeRetryableAPIError(
  error: unknown,
): 'overloaded' | 'server_error' | 'timeout' | 'non_retryable' {
  if (!isHTTPError(error)) return 'non_retryable'

  switch (error.status) {
    case 529: return 'overloaded'   // API 过载，可重试
    case 500: return 'server_error' // 服务器错误，可重试
    case 408: return 'timeout'      // 请求超时，可重试
    default:  return 'non_retryable'
  }
}

// Prompt 太长错误
export const PROMPT_TOO_LONG_ERROR_MESSAGE = 'prompt is too long'
export function isPromptTooLongMessage(msg: Message): boolean {
  return msg.type === 'assistant' && msg.apiError === 'prompt_too_long'
}
```

### 2.3 withRetry.ts — 指数退避重试

```typescript
// src/services/api/withRetry.ts

export async function withRetry<T>(
  fn: () => Promise<T>,
  options: {
    maxAttempts?: number  // 默认 3
    initialDelayMs?: number  // 默认 1000ms
    onRetry?: (attempt: number, error: Error) => void
  } = {},
): Promise<T> {
  const { maxAttempts = 3, initialDelayMs = 1000 } = options

  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try {
      return await fn()
    } catch (e) {
      const category = categorizeRetryableAPIError(e)
      if (category === 'non_retryable' || attempt === maxAttempts) {
        throw e
      }

      // 指数退避：1s, 2s, 4s, ...
      const delay = initialDelayMs * Math.pow(2, attempt - 1)
      options.onRetry?.(attempt, toError(e))
      await sleep(delay)
    }
  }
  throw new Error('Max retries reached')
}

// FallbackTriggeredError：触发降级模型时抛出
export class FallbackTriggeredError extends Error {
  constructor(public readonly fallbackModel: string) {
    super(`Falling back to ${fallbackModel}`)
  }
}
```

### 2.4 dumpPrompts.ts — 请求调试

```typescript
// src/services/api/dumpPrompts.ts

// 调试模式：将完整 API 请求保存到文件
// 环境变量：CLAUDE_CODE_DUMP_PROMPTS=/tmp/dumps

export function createDumpPromptsFetch(
  originalFetch: FetchLike,
  dumpPath: string,
): FetchLike {
  return async (url, init) => {
    // 保存请求
    if (dumpPath) {
      await writeFile(
        path.join(dumpPath, `request-${Date.now()}.json`),
        JSON.stringify({ url, body: JSON.parse(init?.body as string) }, null, 2),
      )
    }
    return originalFetch(url, init)
  }
}
```

---

## 三、Compact 服务 — Context 压缩

### 3.1 autoCompact.ts

```typescript
// src/services/compact/autoCompact.ts

export type AutoCompactTrackingState = {
  hasCompacted: boolean
  compactedAt?: number
}

// 检查是否需要压缩
export function isAutoCompactEnabled(): boolean {
  return getFeatureValue_CACHED_MAY_BE_STALE('AUTO_COMPACT') !== false
}

// Token 警告状态
export function calculateTokenWarningState(
  contextTokens: number,
  maxContextTokens: number,
): 'ok' | 'warning' | 'critical' {
  const ratio = contextTokens / maxContextTokens
  if (ratio < 0.7) return 'ok'
  if (ratio < 0.85) return 'warning'
  return 'critical'  // → 触发压缩
}
```

### 3.2 compact.ts — 压缩实现

```typescript
// src/services/compact/compact.ts

// 生成压缩后的消息列表
export async function buildPostCompactMessages(
  messages: Message[],
  options: CompactOptions,
): Promise<Message[]> {
  // 1. 确定压缩范围（保留最近 N 条完整消息）
  const { toCompress, toKeep } = splitMessagesForCompact(messages, options)

  // 2. 调用 LLM 总结历史（使用轻量模型）
  const summary = await summarizeMessages(toCompress, {
    model: COMPACT_MODEL,  // 使用专用的压缩模型
    systemPrompt: COMPACT_SYSTEM_PROMPT,
  })

  // 3. 创建压缩边界消息
  const boundary = createMicrocompactBoundaryMessage(summary)

  // 4. 拼接：[summary + boundary + toKeep]
  return [
    createUserMessage(summary),   // 历史摘要
    boundary,                      // 边界标记
    ...toKeep,                     // 保留的最近消息
  ]
}

// 压缩边界消息（特殊类型，用于识别压缩位置）
export function createMicrocompactBoundaryMessage(summary: string): SystemCompactBoundaryMessage {
  return {
    type: 'system',
    subtype: 'compact_boundary',
    content: summary,
    timestamp: Date.now(),
  }
}
```

---

## 四、Analytics 服务 — 埋点系统

### 4.1 类型安全的埋点

```typescript
// src/services/analytics/index.ts

// 类型名本身就是使用限制（编码在命名约定中）
export function logEvent(
  eventName: string,
  // 特殊类型名：强制开发者确认不包含代码或文件路径
  metadata: AnalyticsMetadata_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS,
): void {
  // 发送到内部 analytics 系统
  sendAnalyticsEvent({
    event: eventName,
    properties: metadata,
    sessionId: getSessionId(),
    userId: getUserId(),
    timestamp: Date.now(),
  })
}

// 示例使用
logEvent('tool_use', {
  toolName: 'BashTool',
  duration: 1234,
  success: true,
  // ❌ 不能传入：command: 'cat /etc/passwd'（可能含敏感信息）
})
```

### 4.2 GrowthBook — Feature Flags

```typescript
// src/services/analytics/growthbook.ts

// 获取 Feature Flag 值（可能是过期的缓存值）
export function getFeatureValue_CACHED_MAY_BE_STALE<T>(
  featureName: string,
  defaultValue?: T,
): T {
  return growthbook.getFeatureValue(featureName, defaultValue)
}

// 使用示例
const compactEnabled = getFeatureValue_CACHED_MAY_BE_STALE('AUTO_COMPACT', true)
const maxContextSize = getFeatureValue_CACHED_MAY_BE_STALE('MAX_CONTEXT_TOKENS', 200_000)
```

---

## 五、记忆系统 — 多层记忆

### 5.1 memdir — CLAUDE.md 记忆目录

```typescript
// src/memdir/memdir.ts

// 加载项目记忆（CLAUDE.md）
export async function loadMemoryPrompt(cwd: string): Promise<string> {
  // 查找层级：cwd → cwd/.. → cwd/../.. → ... → 项目根
  const claudeMdFiles = await findClaudeMdFiles(cwd)

  return claudeMdFiles
    .map(f => readFileSync(f, 'utf-8'))
    .join('\n\n---\n\n')
}

// 嵌套记忆附件（NestedMemory）
// 当 Agent 进入子目录时，自动注入该目录的 CLAUDE.md
export async function getNestedMemoryAttachment(
  path: string,
  context: ToolUseContext,
): Promise<AttachmentMessage | null> {
  // 检查是否已注入（防止重复）
  if (context.loadedNestedMemoryPaths?.has(path)) return null

  const claudeMd = join(path, 'CLAUDE.md')
  if (!existsSync(claudeMd)) return null

  context.loadedNestedMemoryPaths?.add(path)
  return createAttachmentMessage('nested_memory', readFileSync(claudeMd, 'utf-8'))
}
```

### 5.2 SessionMemory — 动态记忆提取

```typescript
// src/services/SessionMemory/

// 在会话过程中，AI 自动提取和记录重要信息
export async function extractAndSaveMemory(
  messages: Message[],
  context: ToolUseContext,
): Promise<void> {
  // 使用轻量 AI 模型分析对话，提取值得记住的内容
  const memories = await extractMemoriesFromConversation(messages)

  if (memories.length > 0) {
    // 保存到 ~/.claude/memory/ 或 CLAUDE.md
    await saveMemoriesToFile(memories, context)
  }
}
```

### 5.3 extractMemories — AI 驱动的记忆提取

```typescript
// src/services/extractMemories/

// 用 LLM 分析对话，提取值得保存的内容
async function extractMemoriesFromConversation(
  messages: Message[],
): Promise<Memory[]> {
  const response = await callLightweightModel({
    systemPrompt: EXTRACT_MEMORIES_PROMPT,
    userMessage: formatMessagesForExtraction(messages),
  })

  return parseMemoriesFromResponse(response)
}
```

---

## 六、AgentSummary — 子 Agent 摘要

```typescript
// src/services/AgentSummary/agentSummary.ts

// 子 Agent 完成后，自动生成摘要
export async function startAgentSummarization(
  agentId: AgentId,
  agentMessages: Message[],
  context: ToolUseContext,
): Promise<void> {
  // 异步进行（不阻塞主流程）
  const summaryPromise = generateAgentSummary(agentMessages)

  // 摘要完成后更新 AppState
  summaryPromise.then(summary => {
    context.setAppState(prev => ({
      ...prev,
      agentSummaries: new Map([...prev.agentSummaries, [agentId, summary]])
    }))
  })
}
```

---

## 七、PromptSuggestion — 实时建议

```typescript
// src/services/PromptSuggestion/promptSuggestion.ts

// 分析当前对话，建议下一步操作
export async function generatePromptSuggestion(
  messages: Message[],
  context: ToolUseContext,
): Promise<string | null> {
  if (!shouldEnablePromptSuggestion()) return null

  // 使用轻量模型分析
  const suggestion = await callLightweightModel({
    systemPrompt: SUGGESTION_SYSTEM_PROMPT,
    userMessage: getRecentContext(messages),
  })

  return suggestion?.trim() || null
}

// UI hook
export function usePromptSuggestion(): string | null {
  // src/hooks/usePromptSuggestion.ts
  // 在用户停止输入时触发建议生成
}
```

---

## 八、toolUseSummary — 工具使用摘要

当对话历史压缩时，工具调用也需要被总结：

```typescript
// src/services/toolUseSummary/toolUseSummaryGenerator.ts

// 为一组工具调用生成摘要
export async function generateToolUseSummary(
  toolUses: ToolUseBlock[],
  toolResults: ToolResultBlockParam[],
): Promise<string> {
  // 格式化工具调用记录
  const callLog = toolUses.map((use, i) => ({
    tool: use.name,
    input: summarizeInput(use.input),
    result: summarizeResult(toolResults[i]),
  }))

  // 生成可读摘要
  return formatToolUseSummary(callLog)
}

// 创建工具使用摘要消息（插入到压缩边界附近）
export function createToolUseSummaryMessage(summary: string): ToolUseSummaryMessage {
  return {
    type: 'tool_use_summary',
    content: summary,
    timestamp: Date.now(),
  }
}
```

---

## 九、plugins 服务 — 插件系统

```typescript
// src/services/plugins/

// 插件加载器
export async function loadAllPluginsCacheOnly(): Promise<LoadedPlugin[]> {
  const pluginDirs = getPluginSearchPaths()

  return Promise.all(
    pluginDirs.flatMap(dir =>
      findPluginManifests(dir).map(manifest => loadPlugin(manifest))
    )
  )
}

// 插件可以注册：
// - 新工具（addTool）
// - 新命令（addCommand）
// - 新 hooks（addHook）
// - 新 Agent 定义（addAgentDefinition）

// 企业安全策略
export function isRestrictedToPluginOnly(): boolean {
  // 如果开启，只有来自已审批插件的工具才能使用
  return getEnterprisePolicy('plugin_only_mode') === true
}
```

---

## 十、tokenEstimation — Token 估算

```typescript
// src/services/tokenEstimation.ts

// 在不调用 API 的情况下估算消息 token 数
export function estimateTokenCount(text: string): number {
  // 粗略估算：约 4 字符 = 1 token（英文）
  // 中文/日文：约 1-2 字符 = 1 token
  return Math.ceil(text.length / 3.5)
}

// 从 API 响应中获取精确 token 数
export function finalContextTokensFromLastResponse(
  messages: Message[],
): number | undefined {
  const lastAssistant = [...messages].reverse().find(m => m.type === 'assistant')
  return lastAssistant?.usage?.input_tokens
}
```

---

## 十一、notifier — OS 通知

```typescript
// src/services/notifier.ts

// 发送系统级通知（任务完成时）
export function sendOSNotification(opts: {
  message: string
  notificationType: string
}): void {
  if (isITerm2()) {
    sendITerm2Notification(opts)  // iTerm2 特有协议
  } else if (isKitty()) {
    sendKittyNotification(opts)   // Kitty 终端
  } else if (isGhostty()) {
    sendGhosttyNotification(opts) // Ghostty 终端
  } else {
    // 回退到 terminal bell
    process.stdout.write('\x07')
  }
}
```

---

*上一章：[07-MCP 集成](./07-MCP集成.md) | 下一章：[09-UI 层](./09-UI层.md)*
