# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
# main.js is kept tracked as distributable artifact for manual installation

# TypeScript cache
*.tsbuildinfo

# Runtime data (plugin settings, generated per-vault)
data.json

# esbuild dependency cache
.dep.json

# OS artifacts
.DS_Store
Thumbs.db
