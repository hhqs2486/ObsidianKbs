审查回归算法
上一章介绍了如何审查分类算法，并介绍了六种不同的分类算沽，还用同一个数据
集按照相同的方式对它们做了审查，本章将用相同的方式对回归算法进行审查。在本章
将学到：
· 如何审查机器学习的回归算法。
．如何审查四种线性分类算法。
· 如何审查三种非线性分类算法。
13.1 
算法概述
本章将审查七种回归算法。首先介绍四种线性算法：
．线性回归算法。
· 岭回归算法（脊回归算法）。
．套索回归算法。
· 弹性网络（Elastic Net ）回归算法。

13 
审查回归算法－「一
然后介绍三种非线性算法：
• K 近邻算法CKNN ）。
· 分类与回归树算法。
．支持向量机
C SVM ）。
本章将使用波士顿房价的数据集来审查回归算法，采用10 折交叉验证来分离数据，
并应用到所有的算法上。另外，还会通过均方误差来评估算法模型。
scikit-
am 中的
cross_ val_score（）函数能够帮助评估算法模型，我们就用这个函数来评估算法模型。
13.2 
线性算法
首先介绍scikit-leam 中用来处理机器学习中的回归问题的四种算法。
13.2.1 
线性回归算法
线性回归算法是利用数理统计中的回归分析，来确定两种或两种以上变量间相互依
赖的定量关系的一种统计分析方法，运用十分广泛。其表达式为y =w'.x坪，
e 表示误差
服从均值为0 的正态分布。在回归分析中，只包括一个自变量和一个因变量，且二者的
关系可用一条直线近似表示，这种回归分析称为一元线性回归分析。如果回归分析中包
括两个或两个以上的自变量，且因变量和自变量之间是线性关系，则称为多元线性回归
分析。在scikit-leam 中实现线性回归算法的是LinearRegression 类。代码如下：
from pandas imp。rt read_csv 
from sklearn .model selection imp。rt KFold 
from sklearn.model_selection imp。rt cross_val_score 
from sklearn.linear_model imp。rt LinearRegression 
曾导入数据
filename ＝’ h。using . csv ’
names = 
［
’ CRIM ’，’ ZN ’，
’ INDUS ’，＇CHAS ’，’ N。x ’，’ RM ’，’ AGE 勺’ DIS ’，
’ RAD 勺’ TAX ’，’ PRTATI。’，’ B ’，’ LSTAT ’ , ’MEDV’ ] 
data = read csv (filename, names=names , delim whitespace=True) 
量将数据分为输入数据和输出结果
array = data.values 
107 .... 

I 机器学习一－Python 实践
X =array[:, 0 :1 3) 
Y = array[: , 13) 
n splits = 10 
seed = 7 
kfold = KFold(n splits ＝口
splits ,
random state=seed) 
model = LinearRegression() 
scoring ＝
’ neg_mean_square<i;二err。z ’
result= cross_val_score(model , X, Y, cv=kfold, scoring=scoring) 
print ( ’ Linear Regression ：屯.3f ’
革
result
. mean())
执行结果如下：
Linear Regression: -34.705 
13.2.2 
岭回归算法
岭回归算法是一种专门用于共线性数据分析的有偏估计回归方法，实际上是一种改
良的最小二乘估计法，通过放弃最小二乘法的无偏性，以损失部分信息、降低精度为代
价，获得回归系数更符合实际、更可靠的回归方法，对病态数据的拟合要强于最小二乘
法。在scikit-leam 中实现岭回归算法的是Ridge 类。代码如下：
fr。m pandas import read_csv 
from sklearn.model_selection imp。rt KFold 
from sklearn.model_selection imp。rt cross_val_score 
fr。m sklearn.linear_model in可port Ridge 
普导入数据
filename ＝
’ h。using.csv ’
names = 
［’ CRIM ’，’ ZN ’，’ INDUS ’，’ CHAS ’，’ N。x ’，’ RM 勺’ AGE 勺’ DIS ’，
’ RAD 勺’ TAX 勺’ PRTATI。’，’ B ’，’ LSTAT',
'MEDV’ ] 
data = read csv(filename, names=names, delim whitespace=True) 
告将数据分为输入数据和输出结果
array = data . values 
X =array[ :, 0:13) 
Y =array[ :, 13) 
n splits = 10 
seed = 7 
kfold = KFold(n splits=n splits , random state=seed) 
model = Ridge () 
... 108 

13 
审查回归算法寸一一
s coring ＝
’ neg_mean_sψare生err。z ’
result= cross val score(model, X, Y, cv=kfold, scoring=scoring) 
R_rint ( 'Ridge Regressi。n ：奄.3f ’
毛
res ult.mean())
执行结果如下：
Ridge Regression: -34.078 
13.2.3 
套索回归算法
套索回归算法和岭回归算法类似，套索回归算法也会惩罚回归系数，在套索回归中
会惩罚回归系数的绝对值大小。此外，它能够减少变化程度并提高线性回归模型的精度。
套索回归算法和岭回归算法有一点不同，它使用的惩罚函数是绝对值，而不是平方。这
导致惩罚（或等于约束估计的绝对值之和）值使一些参数估计结果等于零。使用惩罚值
越大，进一步估计会使缩小值越趋近零。这将导致我们要从给定的n 个变量中选择变量。
如果预测的一组变量高度相似，套索回归算法会选择其中的一个变量，并将其他的变量
收缩为零。在scikit-leam 中的实现类是Lasso 。代码如下：
from pandas imp。rt read_csv 
from sklearn.model selection import KFold 
from sklearn .model_selection imp。rt
cross~val_score 
fr。m sklearn.linear_model imp。rt Lasso 
量导入数据
filename = 
'h。using.csv ’
names = [’ CRIM’,’ ZN’,’ IND OS ’，’ CHAS ’，’ N。x ’，’ RM ’，’ AGE ’，’ DIS ’，
’ RAD ’，’ TAX 勺’ PRTATI。’，’ B ’，’ LSTAT 勺’ MEDV ’］
data= read csv (filename, names=names, delim whitespace=True) 
普将数据分为输入数据和输出结果
array = data.values 
X =array[:, 0:13] 
Y =array [:, 13] 
n_splits = 10 
seed = 7 
kfold = KFold(n splits=n splits , random state=seed) 
model = Lasso() 
scoring ＝
’ neg_1回an_sqi.且ared_err。z ’
109 ~ 

一T 机器学习一－Pytho n 实践
result= cross val score (rnodel , X, Y, cv=kfold, scori ng=scoring) 
print ( ’ Lasso Regressi。n ：
屯.3f ’毛
result.me an())
执行结果如下：
Lasso Regression : -34 . 464 
13.2.4 弹性网络回归算法
弹性网络回归算法是套索回归算法和岭回归算法的混合体，在模型训练时，弹性网
络回归算法综合使用L1 和L2 两种正则化方法。当有多个相关的特征时，弹性网络回归
算法是很有用的，套索回归算法会随机挑选算法中的一个，而弹性网络回归算法则会选
择两个。与套索回归算法和岭回归算法相比，弹性网络回归算法的优点是，它允许弹性
网络回归继承循环状态下岭回归的一些稳定性。另外，在高度相关变量的情况下，它会
产生群体效应；选择变量的数目没有限制；可以承受双重收缩。在scikit-leam 中的实现
类是Elasti cNet。代码如下：
from pandas imp。rt read csv 
from sklearn .rnodel_selection imp。rt KFold 
from sklearn . rnodel_selection imp。rt cross_val_score 
from sklearn . linear_rnodel import ElasticNet 
非寻入数据
filename =’housing.csv ’ 
names = 
［
’ CRIM ’，’ ZN ’，’ INDUS ’，’ CHAS 勺’ N。x ’，’ RM ’ ，’ AGE ’，’ DIS ’ ，
’ RAD ’，’ TAX ’，’ PRTATI。勺’ B ’ ，
’ LSTAT 勺’ MEDV ’
］
data = read csv (filenarne , 
names ＝口arnes ,
delirn whitespace=True ) 
普将数据分为输入数据和输出结果
array = data.values 
X =array[ :, 0 : 13) 
Y = array[ :, 13) 
n splits = 10 
seed = 7 
kfold = KFold(n splits ＝口
splits ,
random state=seed) 
model = Elast 工cNet () 
scori 口g
＝
’ neg_mean_squared_err。z ’
result= cross val score(rnodel , X, Y, cv=kfold, scoring=scoring) 
print ( 'ElasticNet Regression ：奄
. 3f ’
毛
resu l t .mean ()) 
... 110 

13 
审查回归算法寸一一
执行结果如下：
ElasticNet Regression: - 31 . 165 
13.3 非线性算法
接下来我们将介绍
scikit-learn 中机器学习的三种非线性回归算法。这三种算法在分
类算法中也存在，所以就不对算法做重复的介绍，只介绍它们在scikit-learn 中的实现类。
13.3.1 
K 近邻算法
K 近邻算法是按照距离来预测结果。在scikit-leam 中对回归算法的K 近邻算法的实
现类是KNeighborsRegressor。默认的距离参数为闵式距离，可以指定曼哈顿距离作为距
离的计算方式。代码如下：
fr。m pandas imp。rt read_csv 
fr。m sklearn .model_selection imp。rt KFold 
fr。m sklearn.model_selection imp。rt cross_val_score 
from sklearn .neighbors imp。rt KNeighborsRegressor 
普导入数据
filename 
z
’housing.csv ’ 
names = 
［
’ CRIM ’，’ ZN ’，’ INDUS 勺’ CHAS ’，’ NOX ’，’ RM ’，’ AGE ’，’ DIS ’，
’ RAD ’，’ TAX 勺’ PRTATI。’，’ B 勺’ LSTAT’,’MEDV’] 
data = read_csv(filename, 
names ＝口ames ,
delim_whitespace=True) 
若将数据分为输入数据和输出结果
array = data.values 
X =array[ :, 0 :1 3] 
Y = array [:, 13] 
n splits = 10 
seed = 7 
kfold = KFold(n_splits=n_splits , random_state=seed) 
model = KNeighborsRegressor() 
scoring ＝
’ neg_mean_squared_err。z ’
result= cross_val_score(model , X, Y, cv=kfold, scoring=scoring) 
print （
’ KNeighb。rs
Regressi。n ：屯.3f ’
毡
res ult.mean())
111 ~ 

一寸机器学习一－Python 实践
执行结果如下：
KNeighbors Regression : -107 . 287 
13.3.2 
分类与回归树
在上一章中已经介绍过分类与回归树的算法，它同样适用于回归问题。在scikit-learn
中分类与回归树的实现类是DecisionTreeRegressor。代码如下：
fr。m pandas imp。rt read_csv 
from sklearn.model selection imp。rt KFold 
fr。m sklearn.model_selection import cross_val_score 
from sklearn.tree import Decis ionTreeRegressor 
普导入数据
filename = ’ housing . csv ’ 
names = [ ' CRIM ’，’ ZN ’ ，’ INDUS ’，
’ CHAS ’ ，’ N。x ’，
’ RM 勺’ AGE ’，’ DI S ',
’ RAD ’ ，’ TAX ’ ，
’ PRTATI。勺
’ B ’，’ LSTAT 勺’ MEDV ’］
data = read csv(filename , names=names, delim wh 工tespace=True )
普将数据分为输入数据和输出结果
array = data.values 
X =array[:, 0:13] 
Y =array[: , 13] 
n_splits = 10 
seed = 7 
kfold = KFold (n splits=n splits , random state=seed) 
model = DecisionTreeRegressor() 
scoring ＝’ neg_mean_squared_e rr。z ’
result= cross val score(model , X, Y, cv=kfold, scoring=scoring) 
print （’ CART ：
屯.3f ’告
result.mea口（））
执行结果如下：
CART : -37.560 
13.3.3 
支持向量机
支持向量机的算法在上一章也介绍过了，它同样可以用来处理回归问题。在
scikit-leam 中处理回归问题的实现类是SVR。代码如下：
‘ 112 

13 
审查回归算法γ一
from pandas imp。rt read csv 
from sklearn.model_selection imp。rt KFold 
from sklearn.model_selection imp。rt cross_val_score 
from sklearn . svm imp。rt SVR 
非寻入数据
filename = ’ housing.csv ’ 
names = 
［’ CRIM ’，’ ZN 勺’ INDUS 勺’ CHAS ＇，’ N。x ’，’ RM ’，’ AGE ’，’ DIS ’，
’ RAD 勺’ TAX 勺’ PRTATI。’，’ B 勺’ LSTAT 勺’ MEDV ’］
data = read csv(filename, names=names , delim whitespace=True) 
善将数据分为输入数据和输出结果
array = data.values 
X = a r r ay ［：，
。：13]
Y=ar r ay[:, 13] 
n splits = 10 
seed = 7 
kfold = KFold(n splits=n splits, random state=seed) 
model = SVR () 
scoring ＝
’ neg_mean_s伊ared_err。z ’
resul t
= cross val score(model , X, Y, cv=kfold, scoring=scoring) 
print ( ' SVM ：屯.3f ’
毡
result
. mean() ) 
执行结果如下：
SVM: - 91.048 
13.4 
总结
在scikit-leam 中，算法评估矩阵的计算经常使用
cross val score 函数，通过指定
scoring 参数来选择使用的不同评估矩阵。
scoring 的参数（摘自
scikit-leam 的使用指南）
如表13-1 所示。
表13-1
Scoring 
Function 
Comment 
Classification 
Accuracy 
metncs accuracy_ score 
average _precision 
me位ics.average_precision score 
113 ... 

一T 机器学习一－Python 实践
续表
Scoring 
Function 
Comment 
fl 
metrics. fl score 
for binary targets 
fl micro 
metrics.fl score 
micro-averaged 
fl macro 
metrics.fl score 
macro-averaged 
fl_ weighted 
me位ics.fl score 
weighted average 
fl_samples 
metrics.fl score 
by multilabel sample 
neg_ log_ loss 
metrics.log_ loss 
requires predict_proba support 
prec1s10n etc. 
me位ics.prec1s1on_score 
suffixes apply as with fl 
recall etc. 
metrics.recall score 
suffiαes apply as with fl 
roc auc 
me位ics.roc auc sc咱re
Clustering 
adjusted_ mutual_ rnfo _score 
metrics.adjusted _mutual_info score 
adjusted_rand_score 
metrics.adjusted _rand_ score 
completeness_ score 
metrics.completeness_ score 
fowlkes mallows score 
metrics.fowlkes mallows score 
homogeneity _score 
metrics.homogeneity_ score 
mutual info score 
metrics.mutual info score 
normalized mutual info score 
metrics.normalized mutual info score 
v measure score 
口1e位ics.v measure score 
Regression 
exp lamed_ vanance 
metrics.explained_ variance_ score 
口eg_mean_absolute_error
metrics.mean absolute eηur 
neg_mean_squ缸ed_error
metrics mean_squared_eπ·or 
neg一mean_squared_ log_ eπor 
metrics mean_ squared_ log_ eπor 
neg_ median_ absolute_ eπor 
metrics.median absolute eηor 
r2 
me仕ics.r2 score 
本章对回归算法进行了审查，使用同一个数据集评估了不同的回归算法，这为处理
回归问题进行算法选择时提供了有效的方法。本章介绍了四种线性算法和三种非线性算
沽，并了解了如何比较选择回归算法。下一章将学习如何通过设计实验选择合适的算法。
i.... 114 
