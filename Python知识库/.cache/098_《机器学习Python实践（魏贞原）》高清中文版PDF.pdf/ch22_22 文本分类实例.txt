文本分类实例
前面介绍了对数值问题的分类与回归问题的实例。在实际应用中还有一类问题比较
重要，那就是文本分类。接下来将会通过一个例子介绍文本分类。
· 如何端到端地完成一个文本分类问题的模型。
· 如何通过文本特征提取生成数据特征。
· 如何通过调参提高模型的准确度。
· 如何通过集成算法提高模型的准确度。
22.1 
问题定义
在这个项目中会采用20 Newgroups 的数据（http://qwone.com/~jason/20Newsgroups/ ), 
这是网上非常流行的对文本进行分类和聚类的数据集。数据集中的数据分为两部分，一
部分是用来训练算法模型的数据，一部分是用来评估算法的新数据。网站上还提供了
3
个数据集，这里采用20news-bydate 这个数据集进行项目研究。这个数据集是按照日期进
行排序的，井去掉了部分重复数据和header，共包含18846 个文档。

22 
文本分类实例寸一一
22.2 
导λ数据
这里使用scikit-learn 的loadfiles 导入文档数据，文档是按照不同的分类分目录来保
存的，文件目录名称即所属类别，文件目录结构如图22” l 所示。
一imL . py
• 
alt.atheism 
• 
嗣20news-bydate-test
" • c。mp .graphics
• 
• ．”田町'1'mil'lif.ll,-:ill!lffl~ 'JI 
textanalyze.py 
幅comp.asm叫lndows .misc
• 
• c。mp . sys . ibm . pc.hardware
• 
• c。mp . sys.mac . hardware
• 
• c。mp.wind。ws . x
.. 
• 
mlsc.forsale 
• 
•
rec.autos 
、，
• 
rec.motorcycles 
.. 
• 
rec . sp。rt . baseball
.. 
• 
rec.sport hockey 
. 
•
sci.crypt 
,. 
• 
sci.electronics 
.. 
•
sci.med 
.. 
” sci.space 
” 
幅soc . rellg ion .chrlstian
” 
/ ’ 
• 
talk.politics.guns 
.. 
• 
talk.polltics.mideast 
., 
• 
talk.politics.misc 
” 
• 
talk.religion.misc 
” 
图22-1
在导入文档数据之前，要导入项目中所需要的类库。代码如下：
fr。m sklearn.datasets imp。rt load_files 
from sklearn.feature_extraction . text in唱port CountVectorizer 
fr。m sklearn.feature extraction . text imp。rt TfidfVectorizer 
from sklearn.linear_model imp。rt LogisticRegression 
from sklearn.naive_bayes imp。rt MultinomialNB 
from sklearn.neighbors imp。rt KNeighborsClassifier 
from sklearn.svm imp。rt SVC 
from sklearn.tree imp。rt DecisionTreeClassifier 
from sklearn.metrics imp。rt classification_report 
from sklearn.metrics imp。rt accuracy_score 
from sklearn.model_selection imp。rt cross_val_score 
from sklearn.model_selection imp。rt KFold 
from sklearn.model selection imp。rt GridSearchCV 
193 ..11111 

一寸机器学习一－Python 实践
善导入数据
from sklearn . ensemble imp。rt AdaBoostClassifier 
from sklearn . ensemble imp。rt RandomForestClassif ier 
fr。m matplotlib ir吨p。rt pyplot as plt 
categories = [ ’alt.atheism ’, 
’ rec.sp。rt . h。ckey ’，
’ c。，mp. graphics ’, 
’ sci.crypt’, 
’ c。mp. 。s .ms-windows.misc ’, 
’ sci.electr。nics ’，
’comp.sys.ibm.pc.hardware ’, 
’ sci.med’, 
’comp.sys.mac.hardware ’, 
’ sci.space ’ , 
’ c。E唔. windows.x ’，
’ s。c.religion.christian ’，
’ misc.f。rsale ’, 
’ talk.p。litics.guns ’，
’ rec.autos ’ 
’ talk.p。litics.mideast ’，
’ rec.mot。rcycles ’，
’ talk.politics.misc ’, 
’ rec.sp。rt.baseball ’, 
’ talk.religi。n.misc ’] 
普导入i/11练数据
train_path = ’ 20news-bydate-train ’ 
dataset train =load files(container path=train path, 
＋
』
s 
e 
＋
ι
－
 
e 
＋
ι
·
 
也
)
y 
s
b 
e
- 
-l
s 
r
w 
o
e 
q
n 
e
nu 
＋
」
’
’
－
a
, 
c
e
。
=
H3
= 
S
曲
”
e
h
刀
h
i
乒
k
t
E
草
f
a
0.Jp 
qA
- 
eEJt 
t
E
飞
s
ae 
c
a
γ
←
」
dataset test= load files (container path=test path, 
categories=categories) 
利用机器学习对文本进行分类，与对数值特征进行分类最大的区别是，对文本进行
分类时要先提取文本特征，相对于之前的项目来说，提取到的文本特征属性的个数是巨
大的，会有超过万个的特征属性，甚至会超过10 万个。
.. 194 

22 
文本分类实例寸一一
22.3 
文本特征提取
文本数据属于非结构化的数据，一般要转换成结构化的数据才能够通过机器学习算
法进行文本分类。常见的做法是将文本转换成“文档－词项矩阵”，矩阵中的元素可以使
用词频或TF-IDF 值等。
TF-IDF 值是一种用于信息检索与数据挖掘的常用加权技术。TF 的意思是词频（Term
Frequency ), IDF 的意思是逆向文件频率（Inverse Document Frequency ）。
TF-IDF 的主要
思想是：如果某一个词或短语在一篇文章中出现的频率高，并且在其他文章中很少出现，
则认为此词或短语具有很好的类别区分能力，适合用来分类。TF-IDF 实际上是TF * IDFo 
IDF 的主要思想是：如果包含词条t 的文档越少，也就是n 越小，
IDF 越大，则说明词条
t 具有很好的类别区分能力。如果某一类文档C 中包含词条t 的文档数为m ，而其他类包
含t 的文档总数为k ，显然所有包含t 的文档数n嘀咕，当m 大的时候，n 也大，按照IDF
公式得到的IDF 的值小，这说明该词条t 的类别区分能力不强。但是实际上，如果一个
词条在一个类的文档中频繁出现，则说明该词条能够很好地代表这个类的文本特征，这
样的词条应该被赋予较高的权重，并将其作为该类文本的特征词，以区别于其他类文档。
这就是IDF 的不足之处，在一份给定的文件里，
TF 指的是某一个给定的词语在该文件
中出现的频率，这是对词数（Term Count ）的归一化，以防止它偏向长的文件。IDF 是一
个词语普遍重要性的度量，某一个特定词语的
IDF ，可以由总文件数目除以包含该词语
的文件的数目，再将得到的商取对数得到。
在
sci.kit-learn 中提供了词频和
TF-IDF 来进行文本特征提取的实现，分别是
CountVectorizer 和Tfidffransformer。下面对训练数据集分别进行词频和TF-IDF 的计算。
代码如下：
善数据准备与理解
毒计算词频
count_vect = CountVectorizer(stop_words='engliah’, 
decode_err。r＝’ ign。re')
X train counts = 
count vect.fit transform(dataset train.data) 
195 ~ 

一寸机器学习一－Python 实践
普查看数据维度
print(X train counts . shape) 
词频的计算结果如下：
(10156, 122402) 
接下来计算一下TF-IDF ，代码如下：
普计算TF- IDF
tf_transformer = TfidfVectorizer(stop_words= 'english’ , 
decode error ＝’ ign。re ’）
X train counts tf = 
tf transformer . fit transform (dataset train.data) 
普查看数据维度
print(X_train_counts_tf . shape) 
TF-IDF 的计算结果如下：
(10156 , 122402) 
这里通过两种方法进行了文本特征的提取，并且查看了数据维度，得到的数据维度
还是非常巨大的。在后续的项目中，将使用TF-IDF 进行分类模型的训练。因为，TF-IDF
的数据维度巨大，并且自用提取的特征数据，进一步对数据进行分析的意义不大，因此
只简单地查看数据维度的信息。接下来将进行算法评估。
22.4 
评倍算法
通过简单地查看数据维度，不能确定哪个算法对这个问题比较有效。下面将采用10
折交叉验证的方式来比较算法的准确度，以便找到处理问题最有效的两三种算法，然后
进行下一步的处理。代码如下：
普设置评估算法的基准
num folds = 10 
seed = 7 
scoring = ’ accuracy ’ 
接下来将会利用提取到的文本特征TF-IDF 来对算法进行审查，审查的算法如下。
.... 196 

22 
文本分类实例寸一一
线性算法：逻辑回归
（LR ）。
非线性算法：分类与回归树（CART ）、支持向量机（SVM ）、朴素贝叶斯分类器
( MNB ）和K 近邻（KNN ）。
算法模型的初始化代码如下：
普评估算法
善生成算法模型
models= {} 
models [ ’ LR’] = LogisticRegression() 
models [ 'SVM’] = SVC() 
models [’ CART ’] = DecisionTreeClassifier() 
m。dels ［’ MNB ’］
= MultinomialNB () 
models [ 'KNN’) = KNeighb。rsClassifier()
所有的算法使用默认参数，比较算法模型的准确度和标准方差，以便从中选择两三
种可以进一步进行研究的算法。代码如下：
普比较算法
results = [ l 
for key in models: 
kfold = KFold(n_splits=num_folds, random_state=seed) 
cv_results = cross_val_score(models[key], 
X train counts tf, dataset_train.target, cv=kfold, 
scoring=scoring) 
results .append(cv_results) 
print （’毡，：屯f
（毡f ）’
毛
（key,
cv_results.mean(), 
cv results. std() ) ) 
执行结果显示，逻辑回归
（LR ）具有最好的准确度，朴素贝叶斯分类器（MNB ）和
K 近邻（KNN ）也值得进一步的研究。执行结果如下：
LR : 0 . 901636 (0. 010638) 
SVM : 0.047658 (0.004797) 
CART : 0.662171 (0.015739) 
MNB : 0.880760 (0.006831) 
KNN : 0. 797163 (0.011413) 
197 .... 

I 机器学习一－Python 实践
接下来看一下算法每次执行结果的分布情况。这里用箱线图来展示算法结果的分布
情况。代码如下：
告箱线图比较算法
fig = plt. figure() 
fig. suptitle ('Algorithm Comparision’) 
ax= fig.add subplot(lll) 
plt.boxplot(results) 
ax.set xticklabels(models.keys()) 
plt. show () 
执行结果如图22-2 所示，从图中可以看到，朴素贝叶斯分类器的数据离散程度比较
好，逻辑回归的偏度较大。算法结果的离散程度能够反应算法对数据的适用情况，所以
对逻辑回归和朴素贝叶斯分类器进行进一步的研究，实行算法调参。
Algorithm c。mparision
E空F
·‘丽丽·
0.8 
巳主P
5宝宝2
0.6 
0.4 
0.2 
－帽0--
0.0 
LR 
SVM 
CART 
MNB 
KNN 
图22-2
22.5 
算法调参
通过上面的分析发现，逻辑回归（LR ）和朴素贝叶斯分类器（MNB ）算法值得进一
步进行优化。下面对这两个算法的参数进行调参，进一步提高算法的准确度。
h‘ 198 

22 
文本分类实例寸一一
22.5.1 
逻辑回归调参
在逻辑回归中的超参数是C。C 是目标的约束函数，
C 值越小则正则化强度越大。
对C 进行调参，每次给C 设定一定数量的值，如果临界值是最优参数，重复这个步骤，
直到找到最优值。代码如下：
普算法调参
普调参LR
param_grid = {) 
param_grid [ ’c ’ l = [0.1, 5 , 13, 15) 
model= 'LogisticRegression () 
kfold = KFold(n splits=num folds, random state=seed) 
grid = GridSearchCV(estimator=model, 
param_grid=param_grid, scoring=scoring, cv=kfold) 
grid_result = grid.fit(X=X_train_counts_tf, 
y=dataset train .target) 
庐山叭’最优：
H 使用恼’
毡
（grid_result.best_s core_,
grid result .best params )) 
可以看到C 的最优参数是13 ( 13 是通过多次调整param_grid 参数得到的）。执行结
果如下：
最优
：
0.925167388736 使用｛
I c ’: 13) 
22.5.2 
朴素贝叶斯分类器调参
通过对逻辑回归调参，准确度提升到大概0.92 ，提升还是比较大的。朴素贝叶斯分
类器有一个alpha 参数，该参数是一个平滑参数，默认值为1.0。我们也可以对这个参数
进行调参，以提高算法的准确度。代码如下：
＃调参MNB
param_grid = {) 
param_ grid [’ alpha ’ l = [0.001, 0.01, 0.1, 1.5] 
model = MultinomialNB() 
kfold = KFold(n_splits=num_f。lds,
random_state=seed) 
grid = GridSearchCV(estimator=model, 
param_grid=param_grid, 
sc。ring=scoring,
cv=kfold) 
grid_result = grid.fit(X=X_train_counts_tf, 
199 .... 

I 机器学习一－Python 实践
y=dataset tra 工n.target)
print （’最优：H 使用恼’主（grid_result.best_score_, grid_result.best_params_) l 
同样，通过多次调整param_grid ，得到朴素贝叶斯分类器的
alpha 参数的最优值是
0.01 。执行结果如下：
最优：
0.916502560063 使用｛＇alpha ’: 0.01) 
通过调参发现，逻辑回归在C= 13 时具有最好的准确度。接下来审查集成算法。
22.6 
集成算法
除了调参，提高算法准确度的方法是使用集成算法。下面对以下两种集成算法进行
比较，看看能否进一步提高模型的准确度。
·随机森林（盯）。
• AdaBoost CAB ）。
集成算法的评估代码如下：
非集成算法
ensembles= {} 
ensembles [’ RF’] = RandornForestClassifier() 
ensembles [’ AB ’] = AdaBoostClassifier() 
非比较集成算法
results = [] 
for key in ensembles: 
kfold = KFold(n_splits=nurn_folds, randorn_state=seed) 
cv results= cross val score(ensembles[key], 
X train counts tf, dataset train.target, cv=kfold, 
scoring=scoring) 
results.append(cv_results) 
pr 工nt （’屯s
：屯f
（屯f ）’
毛
（key,
cv results.mean(), cv_results.std())) 
对这两个算法的评估结果如下：
RF : 0. 733162 (0. 015826) 
AB : 0. 558684 (0. 011587) 
.... 200 

22 
文本分类实例寸一一
执行结果如图22-3 所示，从箱线图可以看到随机森林（RF ）的分布比较均匀，对数
据的适用性比较高，更值得进一步优化研究。
Alg。rithm c。mparis1。n
0.75 ~ 
品
0.70 
0.65 
0.60 
0.55 ~ 
? 
RF 
AB 
图22-3
22.7 集成算法调参
通过对集成算法的分析，发现随机森林算法具有较高的准确度和非常稳定的数据分
布，非常值得进行进一步的研究。下面通过调参对随机森林算法进行优化。随机森林有
一个很重要的参数n
estimators ，下面对n
estimators 进行调参优化，争取找到最优解。
代码如下：
告澜参RF
param_grid = {} 
param grid[ 'n_estimat。rs ’
l
= [10, 100, 150, 200 ] 
model = RandomForestClassifier() 
kf。ld = KFold(n splits=num folds, random state=seed) 
,qrid = GridSearchCV(estimator=model, 
param_grid=param_grid, scoring=scoring, cv=kfold) 
grid_result = grid.fit(X=X train counts tf, y=dataset train.target) 
p_rint （’最优：毡，使用抽’毛（grid_result. best_ score_, grid_ result. best_params _ ) ) 
201 .All 

~机器学习一－Python 实践
调参之后的最优结果如下：
最优
：
0.867467506892 使用｛’ n estimators ’:
150) 
22.8 
确定最终模型
通过对算法的比较和调参发现，逻辑回归算法具有最高的准确度，因此使用逻辑回
归算法生成算法模型。接下来会利用评估数据集对生成的模型进行验证，以确认模型的
准确度。需要注意的是，为了保持数据特征的一致性，对新数据进行文本特征提取时应
进行特征扩充，下面使用之前生成的tf transformer 的transform 方法来处理评估数据集。
代码如下：
善生成模型
model = LogisticRegression(C=l3) 
rnodel.fit(X train counts tf, dataset train.target) 
X test counts = tf transforrner . transforrn(dataset test.data) 
predictions = rnodel.predict(X_test_counts) 
print(accuracy score(dataset test . target, predictions)) 
print(classification report(dataset test.target, 
predictions)) 
从结果可以看到，准确度大概达到了84% ，与期待的结果比较一致。执行结果如下：
0.844822485207 
precision 
recall fl-score 
support 
。
0 . 84 
0 .7 7 
0.80 
319 
1 
0 .7 4 
0.80 
0.77 
389 
2 
0.77 
0.74 
0.76 
394 
3 
0. 71 
0.74 
0.73 
392 
4 
0.81 
0.85 
0.83 
385 
5 
0.86 
0.77 
0.81 
395 
6 
0.83 
0.91 
0.87 
390 
7 
0.97 
0.97 
0.97 
398 
8 
0.91 
0.94 
0 .92 
397 
9 
0 . 96 
0.97 
0.96 
399 
10 
0.96 
0.93 
0 . 94 
396 
Ill.. 202 

22
文本分类实例才一
11 
0 . 79 
0 . 79 
0 . 79 
393 
12 
0.91 
0.88 
0 . 89 
396 
13 
0.90 
0.92 
0 . 91 
39 4 
14 
0.86 
0.94 
0 . 89 
398 
15 
0 . 75 
0.92 
0 . 83 
364 
16 
0 . 87 
0.61 
0 . 71 
310 
17 
0.75 
0.60 
0 . 66 
251 
avg I total 
0 . 85 
0.84 
0 . 84 
6760 
22.9 总结
本章主要介绍了对文本的分类方法，这类问题可以应用在垃圾邮件自动分类、新闻
分类等方面。在文本分类中很重要的一点是文本特征提取，在进行文本特征提取时可以
进一步优化，以提高模型的准确度。另外，对中文的文本分类，需要先进行分词，然后
利用skleam.datasets.base.Bunch 将分词之后的文件加载到scikit-leam 中。在运用机器学习
处理问题时，需要灵活运用已经掌握的知识，寻找问题的解决方案。
203 .... 

附录A
A.1 
IDE PyCharm 介绍
PyCharm 是由
JetBrains 打造的一款P严hon IDE ，带有一整套可以帮助用户在使用
町thon 语言进行开发时提高效率的工具，比如调试、语法高亮、Pr句ect 管理、代码跳转、
智能提示、自动完成、单元测试、版本控制。此外，该IDE 还提供了一些高级功能，以
用于支持Django 框架下的专业Web 开发。
Py Charm 目前有两个版本：收费的专业版和免费的社区版，如图A-1 所示，用户可
以到PyCharm 的官网自行下载（http://www.jetbrains.com/pycharm/download/ ）。专业版对
利用Python 语言进行Web 开发提供了更多的支持，而社区版为平常的学习开发提供了足
够的功能。本书中提供的示例代码全部使用PyCharm 进行开发。
!
o。wnload PyCharm 
! 
Pr。fesslonal
C。mmunlty
I 
Full-featured IDE 
F。r Py th。n&Web
development 
1 •圈’
图A-1
Lightweight ICE 
f。r Python & Scientific 
development 
·圃’
，肉...…仲.. ”·· 

一寸机器学习一－Python 实践
A.2 
Python 文档
Python 官网提供了丰富的文档（https: //www.python.org/doc／），涵盖从初级到高级的
全部内容，所有的读者都可以找到适合自己的文档，如图A-2 所示。
$Beginner 
• Begmner s Gulde 
·
时怕。，12or3'
·
时巾。n FAQs 
，
拿Python 当X Resources 
$Moderate 
• p川on Pe ＜，。【licals
．时thonS。。ks
• Browse Python 3.6.3 Documentati刷（Module lnde•) 
• What＇”相In 町tho价，.
• Turori•I 
• llbra俨yRef"'"''
• language 街...... 忧’
• E><tend•ng and Emb<ddlng 
’
Py耐。”ICAP!
·
υsmg 的胁。n
· 时thonHOWIOS
• Search the onhne d配S
6 Advanced 
0General 
Python Packa且，ng User 
• PEP lnde翼
Gu•de 
．问thonVid回氢
•
Irνdevelopme何tO。“
• Oevelopefs Guide 
• Gu•do·s Essays 
司II Python 2.x Resources 
• B<owse P灿on2.714D配ument础。” · （Module I叫时
• Wha内n制inPytho伺27
• Tuton•! 
• L•bmy R.i1renc• 
• 
La吨u•geRel阴阳臼
• htendong and Emb<ddlng 
．
时thon/CAPI
·
凶，ng 时’hon
· 即由制”。WT。s
• Search t惘刷lined也S
• Download Cu"ent Oo相mentation (mull<ple format事...
• Oo酬lo回ζurrent D。印刷ntation (multiple fonnalS '" 
, 
available. Including typeset versions for p<lntln创.，.，，.胁，Including type四.•啊，ons for printmg ) 
一一一一一－一一一一一一一－一－一一一一一－
图A-2
A.3 
Sci町、
Num町、
Matplotlib 和Pandas 文档
SciPy 、
NurnPy 、
Matplotlib 和Pandas 的官网提供了丰富的文档，对API 的适用有详
细的使用说明。网址如下：
• 
SciPy一一寸ittps ://www.scipy.org／。
• NurnPy－寸ittp: //www.numpy.org／。
• 
Matplotlib-h句：／／matplotlib . org／。
• 
Pandas－一h句：／／pandas.pydata.org／。
A.4 
树模型可视化
安装graphviz
在Mac 上graphviz 的安装是通过MacPorts 进行的，这样可以自动安装graphviz 的
Ill.. 206 

E牛
所有依赖包。安装MacPorts 时可以到官网（https://www.macports.org／）下载安装文件，
直接安装即可。由于gr叩hviz 依赖xcode 的一些库，需要先安装xcode。安装MacPorts
之后，执行以下命令完成对graphviz 的安装。
sudo port install graphviz-gui 
决策树结果可视化
使用莺尾花数据集演示如何将决策树的结果可视化，数据集可以直接到UCI 机器学
习仓库（http: //archive.ics.uci.edu/rnl/datasets/Iris ）下载。代码如下：
from pandas import read_csv 
from matplotlib .image imp。rt imread 
from matplotlib imp。rt pyplot 
from sklearn.model selection imp。rt train test split 
fr。m sklearn . tree import DecisionTreeClassifier 
from sklearn . tree imp。rt export graphviz 
fr。m sklearn.metrics imp。rt accuracy score 
imp。rt pydotplus 
import os 
告导入数据
filename =’ iris.data.csv ’ 
names 
= [’ sepal-length’,’ sepal-width’,’petal-length’,’petal-width’, 
’ class ’ ] 
dataset = read csv(filename , names=names) 
array = dataset .values 
X = array [ : , O: 4 J 
y = array [ : , 4 J 
非分离数据集
X_tra 工n ,
X_test, y_train , y_test = train_test_split(X, 
y, random state=7 , test size=0 . 2) 
非决策树模型v11练、
model = DecisionTreeClassifier() 
model . fit(X=X train , y=y train) 
告决策树图形化
dot_data = export_graphviz(model , out_fi l e=None) 
graph = pydotplus . graph from dot data(dot data) 
path = os . getcwd () +’ /’ 
207 .All 

一寸机器学习一－Python 实践
tree_file = path +’ iris.png ’ 
try: 
os.remove(tree file) 
except: 
print (’ There is n。
file
t。
be deleted. ’) 
finally: 
graph.write(tree_file, format=’png’) 
) 
e 
14 
·l 
俨
＆
ea 
et 
ra 
td 
de 
aq
) 
ea
, 
rmf 
m·
-
f 
i
（
。
w·
) 
=
o({ 
hsw 
as
--
o 
tmxh 
印
k
a
－
－
a
s
M
H
唱
。
．
骂
－
t
t
t
L
J
可
e
0
0
0
－
歹
q
d
l
l
l
zappp 
tuAmV4VAV4 
#
ippp 
辑评估算法
predictions= model.predict(X test) 
print(accuracy_score(y_test, predictions)) 
生成决策树的算法模型图像时，首先利用scikit-leam 的方法导出
dot 描述，并通过
dot 来生成图像。通过生成的图像，可以很方便地看到在树模型中如何进行分类。执行上
述代码生成的决策树模型如图A-3 所示。
图A-3
‘ 208 

E牛
A.5 
scikit-learn 的算法选择路径
图A-4 是scikit-leam 官网的算法选择路径图的英文翻译，在选择算法时可以将其
作为参考。
图A-4
A.6 
聚类分析
聚类（Cluster ）分析又称群分析，是研究分类（样品或指标）问题的一种统计分析
方法，也是数据挖掘的一个重要算法。聚类分析是由若干模式（Pattern ）组成的，通常，
模式是一个度量
（Measurement ）的向量，或者是多维空间中的一个点。聚类分析以相
似性为基础，在同一个聚类中的模式之间比不在同一个聚类中的模式之间具有更多的
相似性。
许多聚类算法在小于200 个数据对象的小数据集合上工作得很好；但是，一个大规
模的数据库可能包含几百万个对象，在这样的大数据集合样本上进行聚类可能会导致有
偏差的结果。因此需要具有高度可伸缩性的聚类算法。
许多聚类算法擅长处理低维的数据，可能只涉及两维或三维。人类的眼睛在最多三
209 ..... 

一寸机器学习一－Python 实践
维的情况下能够很好地判断聚类的质量。在高维空间中，聚类数据对象是非常有挑战性
的，特别是考虑到这样的数据可能分布非常稀疏，而且高度偏斜。因此，对于多维度的
数据进行聚类算法时首先要进行降维处理。
接下来通过一个例子来展示聚类算法的应用。在这里使用UCI 数据仓库中的
wine
数据集
（http://archive.i 
并且数据被分为三个类别，通过KMean 算法自动聚类。代码如下：
from pandas import read csv 
from sklearn.cluster import KMeans 
from sklearn.decompos 工ti on
工mport PCA 
from sklearn.preprocessing import scale 
from sklearn.preprocessing impor t StandardScaler 
from matplotlib import pyplot as plt 
from mpl toolkits.mplot3d import Axes3D 
import numpy as np 
from sklearn import metrics 
告导入数据
filename =’ wine.data ’ 
names = [’ class ’,’Alcohol ’,’ MalicAcid’,’ Ash ’, 
’ AlclinityOfAsh’,’Magnesium’,’ Total Phenols ’, 
’ Flavanoids ’, 
’ Nonf layanoidPhenols’,’ Proanthocyanins ’, 
’ Colorintens 工yt ’，’ Hue ’，’ OD280 / 0D315 ’，’ Proline ’] 
dataset = read csv(filename, names=names) 
dataset [’ class ’]= 
dataset [’ class ’] .replace(to replace=[l, 2, 3], value=[O, 1 , 2 ]) 
array = dataset.values 
·X =array[:, 1:13] 
y =array[:, OJ 
非数据烽维
pea = PCA(n components=3) 
X scale= StandardScaler() .fit t ransform(X) 
X reduce= pea.fit transform(scale(X scale)) 
善模型训练
Iii.. 210 

E牛
model= KMeans(n clusters=3) 
model. fit (X reduce) 
labels =model.labels 
#print (labels) 
善输出模型的准确度
print （’毛.3f
屯.3f
毛.3f
在.3f
宅.3f
主.3f ’宅（
metrics .homogeneity_score(y, labels), 
metrics.completeness score(y, labels), 
metrics .v_measure_score (y, labels), 
metrics .adjusted rand score(y, labels), 
metrics. adjusted _mutual_ info_ score (y, 
labels), 
metrics.silhouette score(X reduce, labels))) 
非绘制模型的分布图
fig = plt. figure() 
ax= Axes3D(fig, rect=[O, 0, .95, l], elev=48, azim=l34) 
ax.scatter(X reduce[:, OJ, X reduce[:, l], X reduce[:, 
2], c=labels.astype(np.float)) 
plt. show() 
这里仅简单演示聚类的实现方式，代码不做详细讲解，聚类结果如图A-5 所示，执
行结果如下：
0.726 
0.722 
0.724 
0.726 
0.719 
0.413 
2014 
-
- 
2d 
z 
l 
e 
nu 
1 
2 
2J 
a
“
电
2d 
E 
- 
au 
- 
2 
、
3
图A-5
211 ~ 

( R 语言预测实战〉
游皑麟著
定价
79 . 00 元
预测是数据挖掘的主要作用之一，
也是大数据时代的核，心价值所在。
基于R 语言，兼具效率和价值的双重属性。
．罢工二
国国冒
学
l .！~！；」
回帽正翩翩割草吕垂
〈医疗革命一一医学数据挖掘的理论与实践〉
邵学杰著
定价
49.0 0 元
采用大量案例与实证，
重点探讨数据挖掘技术如何与
临床医学深度融合。
欢迎投稿
邮箱：shiqian@phei.com.cn
微信：sq_0223
固自按照1田
崔厦a理画
回驰部照

Btoadview ＂博文视点
· IT出版腹舰晶牌
www.bro.dv’”.com.en 
技术凝聚实力
· 专业创新出版
基于大数据的人工智能如今异常火爆。对于普通人而言，人工智能意味着自动驾驶、电脑医生，
甚至意味着许许多多人类的工作将被机器取代。而对于有志于投身大数据和人工智能领域的传统
IT 人士来说，需要了解这些名词的真正内涵，更加需要了解支撑这个所谓“智能时代”背后的机
制和原理。机器的智能当然不是天生的，需要通过从数据中学习来获得，而Pyth on 是目前数据分
析和机器学习领域处于领先地位的编程语言。本书以初学者的视角，带领读者通过Pyth o n 编程实
践，实际接触和了解机器学习以及Python 语言本身，希望在给读者带去知识和技能的同时，也能
为大家悄悄开启一段驾驭智能时代的旅程。
一一王若平，副合伙人，
旧M 客户创新中心质量保证部负责人
本书面向于有一定编程基础，并且有志于大数据分析、数据挖掘和算法的程序员、项目经理，
以及相关专业的学生。本书不同于教授编程语言的普通教材，而是通过列举很多经典实例，来帮
助读者理解Pyt h on 语言，同时可以使读者理解和延伸对大数据分析与挖掘的认识。未来是一个数
据的世界，希望读者通过这本书能够很好地驾驭数据，给予我们更大的帮助
！
同时感谢作者的辛
勤努力与智慧！
明~~
：~
1 摇摆固自：
＇坠d
！回h放悦｜博文视点B 「oadv1ew
c-.-丁策划编辑：石倩
｜昌亘；1 责任编辑：徐津平
』主』4 封面设计：吴海燕
一一李鸿飞，旧M 高级经理，
Cloud Application Development Leader, Complex SI & A 「ch Leade 「
•a描在骂，~－jJ隐若主型rl~ii!'ilim
ISBN 978-7-12 1-3311 0-7 
定价：
5 9.00元
