# 概念归属约定（嵌入式软硬件开发知识库 · 数据结构模块）

> **作用**：多本数据结构书覆盖同一概念时，预先划分「谁拥有」，避免重复建卡、保持关系图干净。
> **链接一致性铁律**：`[[链接]]` 名必须与目标 `.md` 文件名**完全一致**（含大小写 / 中英文 / 空格）；文件名**不含 `/`**，多词概念用连字符（如 `抽象数据类型ADT`）。
> **本批来源**：`E:\python学习书籍\037.数据结构\` 下 3 本 PDF（均为扫描版，无文字层，已 OCR）。

---

## 〇、已存在的数据结构概念卡（先前 02-数据结构.pdf 学习笔记）

本批**只链接、不重建**，用严蔚敏教材作为权威来源去「丰富」（补来源 / 补细节 / 加双链）：

- `10-🧠核心概念/软件/`：`链表`、`树`、`栈与队列`、`排序与查找`、`数组与字符串`、`时间复杂度`、`递归`、`指针与内存`、`函数与栈`、`结构体与位域`、`联合体`、`位运算`、`预处理与宏`、`C语言`
- `10-🧠核心概念/硬件/`：ADC / CAN / CubeMX / DMA / GPIO / HAL库结构 / I²C / RC滤波 / SPI / UART / USB / 三极管开关电路 / 上拉电阻与下拉电阻 / 中断NVIC / 去耦电容 / 复位电路 / 定时器与PWM / 时钟树 / 晶振与时钟电路 / 电平转换 / 看门狗 / 线性电源LDO / 开关电源DCDC

---

## 一、严蔚敏《数据结构（C语言版）》（理论教材 · 权威 · 本批新建枢纽）

- **拥有（必须建卡，写在 `10-🧠核心概念/软件/` 下）**：
  - `数据结构`（总论：逻辑结构 / 存储结构、为什么学）—— **模块枢纽**
  - `抽象数据类型ADT`
  - `线性表`（含 顺序表 vs 链表 对比，[[链接]]到 `链表`）
  - `图`（图论：存储 / DFS-BFS / 最小生成树 / 最短路径 / 拓扑排序 / 关键路径）
  - `哈希表`（散列表、散列函数、冲突处理；[[链接]]到 `排序与查找`）
  - `KMP算法`（串模式匹配；[[链接]]到 `数组与字符串`）
  - `广义表`（minor，可选）
- **只 [[链接]]（不建）**：`栈与队列`、`树`、`链表`、`排序与查找`、`数组与字符串`、`时间复杂度`、`递归`（均已存在）
- **来源标注**：本书第 X 章（PDF 为图片扫描版，已 OCR + 领域知识整理）

## 二、高一凡《算法实现及解析》（参考代码集 · 不建概念卡）

- **定位**：严蔚敏教材的逐算法 C 语言实现配套。450 页，每页一个算法，编号 1..N；目录仅为页码、无标题，无法按章索引。
- **处理**：仅建 `20-📚资料库/教程/` 概览卡，指向对应理论概念卡。**不抽全文 OCR**（代码 OCR 质量差、价值低）。

## 三、缪淮扣《数据结构——C++实现习题解析与实验指导》（习题解答 · 不建概念卡）

- **定位**：配套习题解答 + 实验指导（无目录）。
- **处理**：仅建 `20-📚资料库/教程/` 概览卡，指向对应理论概念卡。

---

## 跨书枢纽（唯一 owner，避免竞态）

- `数据结构` → 严蔚敏（本批新建的总论卡，作为数据结构模块枢纽）
- `栈与队列` / `树` / `链表` / `排序与查找` / `数组与字符串` / `时间复杂度` → 先前已建，本批只链接、只回链

---

## 二十四、嵌入式深度学习神书（桌面 PDF · 英文原版）

源：`C:\Users\liang\Desktop\嵌入式深度学习神书.pdf`（216 页 / 7 章，文字版，无需 OCR）。
归属（写在 `10-🧠核心概念/AI与边缘计算/` 下，本批**全部新建**——库里此前无任何 AI/深度学习领域卡）：

- 基础 8 张：`机器学习` `深度学习` `神经网络` `卷积神经网络CNN` `循环神经网络RNN` `反向传播` `损失函数` `优化器`
- 部署 8 张（核心）：`嵌入式深度神经网络`(枢纽) `模型量化` `二值神经网络` `模型压缩与稀疏` `分层级联处理` `硬件算法协同优化` `近似计算DVAFS` `神经网络加速器`
- 教程 1 篇：`12-嵌入式深度学习(神书)`
- 地图：`50-🗺️索引MOC/AI与边缘计算地图.md`
- 注：本书是嵌入式端 DL 部署专著（量化/二值/稀疏/近似计算/专用加速器），与 软件/硬件/工具链 子目录均不重叠，故独立建 `AI与边缘计算` 子目录。

---

## 二十五、现代边缘 AI（互联网调研补全 · 2026-07）

源：联网调研（边缘 AI 推理框架对比 / OpenVINO 2025.1 / ExecuTorch 1.0 GA / MCUNet / TinyML 技术报告 / On-Device LLM 2025-2026 指南）。
动机：神书为 2019 年著作，缺失 2020-2026 工业现实。本批**全部新建**，归属 `10-🧠核心概念/AI与边缘计算/`：

- `现代边缘AI推理引擎`（ONNX Runtime / TensorRT / OpenVINO / LiteRT / Core ML / ExecuTorch / TVM）
- `TinyML与MCU部署`（MCUNet / TFLite Micro / CMSIS-NN / MLPerf Tiny）
- `现代边缘AI加速器`（Jetson Orin / Edge TPU / Ethos-U / ANE / Hexagon / Hailo-8）
- `量化感知训练QAT`（PTQ vs QAT；PyTorch / Intel Neural Compressor / NNCF / optimum-intel）
- `端侧大语言模型`（llama.cpp / Ollama / MLX / ExecuTorch / LiteRT / Core ML；Phi-4-mini / Gemma 3 / Llama 3.2；GGUF/GPTQ/AWQ）
- 地图新增「三、现代边缘 AI（联网补全）」；`模型量化` / `神经网络加速器` 已补现代工具回链。
- 注：本批内容为时效性强的工程事实，\n- 补建 2 张被引用的真实概念卡（修复断链）：`知识蒸馏`（归入二、嵌入式部署段，模型压缩三大件之一）、`CMSIS-NN`（归入三、现代段，Cortex-M 加速库）。总数 16→23。
- 建议每年复核一次大版本（如 ExecuTorch、OpenVINO、端侧小模型）。


---

## 二十六、零基础入门硬件PCB设计（桌面课程资料）

源：`E:\零基础入门硬件PCB设计`（教程PPT 22MB + 6张原理图PDF + 封装库 + 打样文件 + STC32G规格书 + 购买清单xlsx）。
归属（全部新建，写在 `10-🧠核心概念/硬件/` 下，作为 PCB 专题枢纽）：

- PCB 流程/工具：`PCB设计流程`(枢纽) `嘉立创EDA`
- 原理图→PCB 基础：`原理图符号与封装` `封装Footprint` `丝印与位号` `网络标号`
- 物理实现：`焊盘与过孔` `铺铜` `层叠与板层` `布局与布线` `DRC设计规则`
- 高速/电源：`差分对与等长` `阻抗控制` `电源分割`
- 制造：`打样与SMT` `阻容封装选型`
- 教程：`20-📚资料库/教程/13-零基础PCB设计入门`（PPT抽取）
- 板级实战：`20-📚资料库/板级实战/` 下 6 篇（原理图PDF抽取）
- 数据手册：`20-📚资料库/数据手册/STC32G144K246-数据手册`
- 索引：`PCB封装库索引` `打样文件索引` `购买清单汇总(零基础PCB)`
- 地图：`50-🗺️索引MOC/PCB设计地图`
- 链接一致性：PCB概念卡 的 `[[链接]]` 名 必须与上述文件名完全一致；大二进制(241MB LCEDA安装包/36MB规格书/zip)只引用路径不复制进库。

---

## 二十七、尚硅谷嵌入式项目（桌面课程目录 `E:\尚硅谷嵌入式项目`）

源：6 个完整实战项目（51智能车 / 宠物定位 / 平衡车 / 心电监测仪 / 四轴无人机 / 水质水位监测），含 docx 教程(HAL/寄存器版)、器件手册、原理图、代码。
归属：
- 硬件概念卡（新建 8 张，写在 `10-🧠核心概念/硬件/`）：`51单片机` `直流电机驱动` `超声波测距` `BLE蓝牙` `GPS定位` `心电ECG采集` `ADS1115` `比较器电路`
- 软件概念卡（新建 2 张，写在 `10-🧠核心概念/软件/`）：`滤波算法` `姿态解算`
- 项目笔记（6 篇，写在 `30-🛠️项目实战/尚硅谷/`，由 docx 抽取）：`尚硅谷-51智能车` `尚硅谷-宠物定位器` `尚硅谷-平衡车` `尚硅谷-心电监测仪` `尚硅谷-四轴无人机` `尚硅谷-水质水位监测`
- 地图：`50-🗺️索引MOC/尚硅谷项目地图`
- 资料/代码索引：`20-📚资料库/尚硅谷资料与代码索引`
- 矢量图：3 个 `.drawio`（PID笔记/PID设计/无人机）已复制到 `30-🛠️项目实战/尚硅谷/assets/`
- 链接一致性：所有 `[[链接]]` 名须与本列表完全一致；大二进制（无人机 161MB 代码包）只引用路径不复制；`【网盘分享WPFX.LINK】` 占位文件一律跳过。

---

## 二十八、硬件电路合集（全能学习包 `D:\嵌入式与单片机全能学习包\01-硬件电路合集`）

源：11 GB，615+ 文档（336 PDF + 279 DOCX + 121 ms9 等），分 3 块：硬件工程师入门学习资料（9 大话题）/ 硬件工程师必备资料（25 篇实战）/ 电子电路（1000 份设计参考）。
**合并策略**：文本库只收概念卡 + 结构化索引；视频/大二进制（.rar/.xlsx/.百MB PDF）仅索引外部路径不复制进库（与 PCB/尚硅谷任务一致）。
归属（全部新建，写在 `10-🧠核心概念/硬件/` 下，补齐库内"模拟/硬件工程基础"空白）：
- 模拟：`运算放大器` `模拟电路基础`
- 分立/无源：`无源元件基础` `二极管与光耦` `MOS管与驱动电路` `变压器与继电器`
- 总线/测量：`RS485总线` `示波器与测试测量`
- 合规：`EMC电磁兼容` `硬件设计规范与评审`
- 功率：`IGBT`
- 索引：`20-📚资料库/硬件电路合集资料索引`（25 篇必备文档逐篇标注价值 + 9 话题编目 + 外部路径）
- 地图：`50-🗺️索引MOC/硬件电路合集地图`
- 链接一致性：10 张卡 `[[链接]]` 名须与上列表完全一致；EMC 培训 PDF 有密码 13431，仅索引；`.rar` 压缩包（24/25/28）不进库。

## 二十九、C语言资料包（03-C语言学习资料包）
- 归属 `10-🧠核心概念/软件/`：C语言高效编程技巧、C语言编码规范、C++与C语言的关系
- 归属 `10-🧠核心概念/工具链/`：**编译工具链**（首次填充原本为空的 工具链 子目录）
- 归属 `30-🛠️项目实战/51单片机C例程/`：51单片机C语言模块精讲
- 归属 `20-📚资料库/`：C语言资料包索引.md；已复制华为规范PDF+2txt 到 `20-📚资料库/C语言资料包/`
- 归属 `50-🗺️索引MOC/`：C语言资料包地图.md
- 链接白名单同既有软件卡（C语言/指针与内存/数组与字符串/预处理与宏/位运算/函数与栈/结构体与位域/联合体）；新增仅上述 4 卡名
---

## 三十、STM32 开发工具包（全能学习包 `D:\嵌入式与单片机全能学习包\04-STM32单片机开发工具包`）

源：8.6 GB，15 个子目录，几乎全是 IDE 安装包（Keil 3.2G / 开发工具 2.5G / STM32_PACK 1.4G / IAR 1.3G）、驱动（CH340 / J-LINK / ST-LINK_V2）、下载器（mcuisp / stc-isp / STVP）、串口调试助手（XCOM / SSCOM / SecureCRT / QCOM）、字符取模软件（PCtoLCD2002 / Image2Lcd）、C51 工具、烧录工具。
**合并策略**：大二进制（安装包 / 驱动 / 压缩包）只索引外部路径不复制；仅复制少量小而有价值的文档（QCOM User Guide PDF 761KB + 3 篇 LCD 取模 docx）进库可预览。库已有 `编译工具链` / `STM32F103` / `CubeMX` / `HAL库结构`，故**只补下载与调试、串口、取模三块空白**：
- 工具链概念卡（新建 3 张，写在 `10-🧠核心概念/工具链/`）：`下载器与调试器` `串口调试助手` `字符取模与图片取模`
- 增强既有：`编译工具链` 加「烧录与调试」段并链 `[[下载器与调试器]]`
- 已复制进库：`20-📚资料库/STM32工具包/`（QCOM PDF + 3 篇取模 docx）
- 索引：`20-📚资料库/STM32工具包资料索引.md`（15 子目录编目 + 外部路径）
- 地图：`50-🗺️索引MOC/STM32工具包地图.md`
- 链接一致性：新卡 `[[链接]]` 名须与上列表一致；下载器卡向既有 `[[编译工具链]]` `[[STM32F103]]` `[[CubeMX]]` `[[UART]]` `[[串口调试助手]]` 回链，串口卡向 `[[UART]]` `[[电平转换]]` `[[RS485总线]]` `[[通信协议设计]]` 回链，取模卡向 `[[GPIO]]` `[[STM32F103]]` 回链。

---

## 三十一、STM32 传感器模块合集（全能学习包 `D:\嵌入式与单片机全能学习包\05-STM32传感器模块合集`）

源：348 MB，5 个信盈达实训模块（LED闪烁 / 光敏电阻 / 超声波 / 酒精(MQ-3) / 雨滴），含模块说明 PDF、wmv 视频、STM32F4 标准外设库示例工程（共 221 个 .c，多数为库文件）。
**合并策略**：wmv 视频 / 示例工程二进制**只索引不复制**；复制 4 份小规格 PDF（光敏/雨滴/MQ-3 模块说明 + 雨滴智能窗户综合板原理图，均 <500KB）进库可预览；抽取真实 F4 代码（adc.c 读 AO、tim14/pwm.c 超声波捕获）做 grounding；`LED闪烁` 目录为空（仅对应库内 [[GPIO]] 基础）。库已有 [[超声波测距]]（本批升级 种子→已完成 + 加链），故只补其余 3 模块空白：
- 硬件概念卡（新建 3 张，写在 `10-🧠核心概念/硬件/`）：`光敏电阻传感器` `MQ气体传感器` `雨滴传感器`
- 实战精讲（写在 `30-🛠️项目实战/STM32传感器模块/`）：`STM32传感器模块接口精讲`（5 模块接线 + 真实 F4 代码 + 外设映射：模拟量走 ADC / 数字量走 GPIO / 超声波走定时器捕获）
- 已复制进库：`20-📚资料库/STM32传感器模块/`（4 份模块说明/原理图 PDF）
- 索引：`20-📚资料库/STM32传感器模块资料索引.md`（5 模块路径编目 + 外部路径 + 视频说明）
- 地图：`50-🗺️索引MOC/STM32传感器模块地图.md`
- 链接一致性：新卡 `[[链接]]` 名须与上列表一致；3 卡均向既有 `[[ADC]]` `[[GPIO]]` `[[比较器电路]]` `[[无源元件基础]]` `[[模拟电路基础]]` 回链，精讲向 `[[STM32传感器模块地图]]` `[[STM32传感器模块资料索引]]` 回链。示例基于 STM32F4 标准外设库，与库内 [[STM32F103]]/[[HAL库结构]] 引脚/API 不同但思路通用，已在精讲与索引中明确标注。

---

## 三十二、STM32 项目合集（全能学习包 `D:\嵌入式与单片机全能学习包\06-STM32项目合集`）

源：316 MB，22 个完整 STM32 项目（原理图/PCB/源码/论文，无视频、无网盘占位），覆盖电源/示波器/DSP/心率/物联网/机智云/蓝牙小车/二维码/USB HID 等。
**合并策略**：大二进制（Altium 源文件 / 源码工程 / 17MB DSP 教程 PDF）只索引外部路径不复制；仅复制 4 份小 PDF（数控电源原理图 / 机智云教程 / USB HID CubeMX / NUCLEO-F072RB 配置）进库可预览。库已有大量 STM32 基础卡，故只补 3 张空白卡 + 写 8 篇深度项目笔记：
- 硬件概念卡（新建 1 张）：`心率检测`（PPG/ECG 取 BPM；链 [[心电ECG采集]] [[ADC]] [[滤波算法]]）
- 软件概念卡（新建 2 张）：`数字信号处理DSP`（CMSIS-DSP/FFT；链 [[STM32F103]] [[滤波算法]] [[定时器与PWM]] [[示波器与测试测量]]）、`二维码识别`（解码库；链 [[滤波算法]] [[通信协议设计]] [[物联网终端]]）
- 深度项目笔记（8 篇，写在 `30-🛠️项目实战/STM32项目合集/`，工程师思维 7 段）：数字示波器与DSP / 多功能数控电源 / 数据采集与心率检测 / 物联网户外环境监测 / 机智云智能家居 / 蓝牙控制小车 / 二维码识别 / USB HID 键鼠
- 索引：`20-📚资料库/STM32项目合集资料索引.md`（22 项目编目 + 外部路径）
- 已复制进库：`20-📚资料库/STM32项目合集/`（4 份 PDF）
- 地图：`50-🗺️索引MOC/STM32项目合集地图.md`
- 链接一致性：3 张卡 `[[链接]]` 名须与上列表一致；设计报告为老版 .doc，库内未抽全文。

---

## 三十三、STM32 书籍与芯片手册（全能学习包 `D:\嵌入式与单片机全能学习包\07-STM32单片机书籍&芯片手册`）

源：264 MB，**38 份 PDF**（根目录 30 份 + 子目录 `STM32  8本其它书籍` 8 份），无视频、无网盘占位、无 >50MB 单文件。
**合并策略**：这是"说明书合集"，所有 PDF **只索引外部路径不复制**（避免 Obsidian 卡死，与硬件电路合集一致）；从关键小文档抽真实文字做 grounding。库已有 [[CAN]] [[中断NVIC]] [[STM32F103]] 等基础卡，故只补 4 张空白卡（均写在 `10-🧠核心概念/硬件/`）：
- `芯片选型`（选型方法；链 [[STM32F103]] [[时钟树]] [[HAL库结构]] [[CAN]] [[STM32文档体系]]）
- `CAN过滤器`（bxCAN 过滤器 屏蔽/列表模式；链 [[CAN]] [[STM32F103]] [[GPIO]] [[通信协议设计]]）
- `中断优先级分组`（NVIC PRIGROUP 5 种分法；链 [[中断NVIC]] [[STM32F103]] [[定时器与PWM]] [[CubeMX]]）
- `STM32文档体系`（数据手册/参考手册/编程手册/固件库手册 各自管什么，元卡；链 [[STM32F103]] [[编译工具链]] [[HAL库结构]] [[CubeMX]] [[下载器与调试器]] 及 [[STM32书籍与芯片手册资料索引]] [[STM32书籍与芯片手册地图]]）
- 索引：`20-📚资料库/STM32书籍与芯片手册资料索引.md`（38 份 PDF 按文档类型分组 + 外部路径，动态生成）
- 地图：`50-🗺️索引MOC/STM32书籍与芯片手册地图.md`
- 链接一致性：4 张卡 `[[链接]]` 名须与上列表一致；并在既有 [[CAN]] [[中断NVIC]] 卡加回链。硬件地图 65→69。

## 三十四、嵌入式Linux（全能学习包 `D:\嵌入式与单片机全能学习包\08-Linux相关学习资料包`）

源：1.4 GB，**51 文件**（30 本 PDF 书 + 4 份 docx 笔记 + 思维导图 PNG/XMind）。无视频、无 WPFX、无 >50MB 单文件（最大 PDF 约 100MB 级，整体只索引不复制）。
**合并策略**：30 本 PDF（1.4G）**只索引外部路径不复制**（与硬件电路合集一致）；从 4 份 docx 抽真实文字做 grounding；路线图 PNG 小图**复制进库**（`20-📚资料库/嵌入式Linux学习路线/`）并嵌入笔记。库此前**零 Linux 内容**，故补 6 张空白卡：
- 软件 5：[[嵌入式Linux]]（四大块总览/三阶段学习/误区）、[[uboot与启动流程]]（环境变量 bootcmd/bootargs、命令）、[[设备树DeviceTree]]（dts/dtb、compatible 匹配）、[[Linux文件IO与系统调用]]（fd/open flag/标准IO vs 文件IO）、[[Linux驱动与内核模块]]（字符设备框架/模块机制）
- 工具链 1：[[交叉编译与根文件系统]]（arm-linux-gnueabihf/sysroot/busybox/init/NFS）
- 索引：`20-📚资料库/嵌入式Linux资料索引.md`（30 书按 8 类编目 + 外部路径，动态生成；docx/思维导目标注）
- 路线：`20-📚资料库/嵌入式Linux学习路线.md`（嵌入 6 张路线图 PNG）
- 地图：`50-🗺️索引MOC/嵌入式Linux地图.md`
- 链接一致性：6 张卡 `[[链接]]` 名须与上列表一致；并在既有 [[Bootloader]] [[编译工具链]] 卡加回链。**软件地图 36→41**；工具链卡列入本包地图（全库无独立工具链地图）。

## 三十五、嵌入式面试题合集（全能学习包 `D:\嵌入式与单片机全能学习包\09-嵌入式面试题合集`）

源：1.5 GB，**2125 文件**，3 子目录：`175套经典嵌入式面试题`(459M, 约175公司真题夹，大量 jpg 截图+少量答案)、`嵌入式面试资料`(634M, 按知识点整理的小 PDF/docx + `50套大厂高频面试题`/`linux` 子目录)、`嵌入式面试题`(388M, 精选大题集 docx/pdf)。无 WPFX；3 个 `.baiduyun.p.downloading` 残缺文件已跳过；无 >100MB 单文件。
**合并策略**：体量巨大且多为 jpg 截图/公司真题，**全部只索引外部路径不复制**；仅复制 5 份小主题 PDF(<5MB，可预览)进 `20-📚资料库/嵌入式面试题合集/`(TCPIP协议栈/ARM体系与架构/CAN总线/I2C总线/SPI子系统；FreeRTOS知识整理 8.3MB 超阈值只索引)。库已有大量概念卡，本包**不建重复主题卡**，只补 6 张面试高频陷阱卡（均为库内此前缺失的考点）：
- `大小端字节序`（union/指针判断、网络字节序；链 [[联合体]] [[通信协议设计]]）
- `volatile与const关键字`（volatile 三例子、const 指针三种、const volatile；链 [[中断NVIC]] [[互斥与信号量]]）
- `static关键字`（三种用法、只初始化一次原因；链 [[函数与栈]] [[编译工具链]]）
- `堆栈与内存布局`（.text/.data/.bss/heap/stack 五段、堆vs栈；链 [[指针与内存]] [[函数与栈]] [[编译工具链]]）
- `进程与线程(Linux)`（fork/exec/pthread、与 RTOS 对比；链 [[嵌入式Linux]] [[任务与调度]] [[互斥与信号量]]）
- `TCP三次握手与四次挥手`（握手/挥手过程+状态+TIME_WAIT；链 [[通信协议设计]] [[嵌入式Linux]]）
- 索引：`20-📚资料库/嵌入式面试题库索引.md`（3 子目录编目 + 外部路径 + 已复制 PDF 标注，动态 walk）
- 地图：`50-🗺️索引MOC/嵌入式面试题地图.md`（6 卡 + 按主题速查链既有卡）
- 链接一致性：6 张卡名须与上列表一致；并在既有 [[联合体]] [[函数与栈]] [[任务与调度]] [[通信协议设计]] [[互斥与信号量]] 卡加回链。**软件地图 41→47**。

---

## 三十六、FreeRTOS 嵌入式开发学习库（GitHub: Despacito0o/FreeRTOS）

源：`D:\GPT浏览器下载\FreeRTOS-main.zip`（2671 文件，含 12 篇中文教程 + Despacito 示例项目 + docs/en/ 英文版）。
库已有 `FreeRTOS` `任务与调度` `互斥与信号量` 三张概念层卡片，本批在实现层补充 9 张新卡（均写在 `10-🧠核心概念/软件/`）：
- `动态任务创建` — xTaskCreate 六参数 + heap_1~5 概述
- `静态任务创建` — xTaskCreateStatic + 空闲任务内存回调
- `队列通信` — xQueueSend/Receive + 环形缓冲区原理
- `二值信号量` — Binary Semaphore 双版本 + FromISR
- `计数型信号量` — Counting Semaphore 资源池管理
- `优先级翻转` — 三任务翻转推演 + 优先级继承 + Mars Pathfinder 案例
- `FreeRTOS 移植` — port 抽象层 + FreeRTOSConfig.h + 中断映射
- `FreeRTOS 内存管理` — heap_1~5 五策略对比 + 选型指南
- `互斥锁 Mutex` — 所有权 + 优先级继承 + 递归互斥量 + 与二值信号量区别
教程（6 篇，编号 14-19，写在 `20-📚资料库/教程/`）：
- `14-FreeRTOS环境搭建与移植`（001+002）
- `15-FreeRTOS任务创建`（003+004+005：动态/静态 + 工程完善）
- `16-FreeRTOS多任务与参数`（006+007）
- `17-FreeRTOS队列通信`（008+009）
- `18-FreeRTOS信号量`（010+011）
- `19-FreeRTOS优先级翻转`（012）
链接一致性：9 张新卡 [[链接]] 名须与上列表完全一致；回链既有 `[[FreeRTOS]]` `[[任务与调度]]` `[[互斥与信号量]]` `[[STM32F103]]` `[[C语言]]`。**软件地图 47→56**。
